# Quality Findings

Statuses are `open`, `in progress`, `closed`, `duplicate`, or `rejected`.
Closing a finding requires a change reference and verification evidence.

| ID | Severity | Unit | Status | Finding | Required resolution |
| --- | --- | --- | --- | --- | --- |
| Q-CODEC-001 | Medium | Q11 | Closed | The embedded MP3 Huffman and synthesis-window tables did not record a reproducible extraction method. The Huffman table named ISO/IEC 11172-3, while the synthesis table named no source. | Commit `6afb44b6` identifies the exact ISO/IEC 11172-3:1993 Annex B tables and records deterministic reconstruction rules. Semantic SHA-256 checks cover every table property and value. A one-bit Huffman mutation failed the focused gate, the restored gate passed 10/10 tests, and the complete MP3 matrix passed 6/6 steps and 130/130 tests. |
| Q-PARSE-001 | Medium | Q10, Q12 | Closed | Vorbis-comment, FLAC-comment, ID3v2.3, ID3v2.4, RIFF INFO, and AIFF text iterators canonically replayed every prior item before each `next`. Complete valid traversal was therefore quadratic in retained item count even though each individual item parser made progress. | Commit `c62ad25e` binds normal traversal to exact source-and-cursor witnesses while retaining canonical fallback for caller-modified state. Forced-fallback and hostile-state tests pass. A checked six-format gate stays flat through 1,024 entries at 151–157 maximum Debug ns per entry and 18 maximum ReleaseSafe ns per entry. The 92-test Vorbis gate, 59-test Q12 selection, focused 36-test selection, and Windows ReleaseSafe cross-compilation pass. |
| Q-IXML-001 | Medium | Q12 | Closed | Public iXML requirements and materialization accepted an unbounded document and derived decoded-text, track, and sync-point counts without an explicit application policy. The parser was allocation-free and structurally depth-bounded, but callers could not cap its complete input-driven work independently of storage. | Commit `c1e49fc0` adds bounded defaults and public custom limits for bytes, conservative structural work, decoded text, tracks, and sync points. Limit rejection precedes materialization. The focused suite passes 19/19 tests, the native fuzz campaign passes 100,528 executions, the installed package passes 18/18 steps and 96/96 tests, and Windows ReleaseSafe cross-compilation passes. |
| Q-MP3-001 | Medium | Q11 | Closed | MP3 memory and positional-file scans bounded individual frame discovery but accepted an unbounded whole stream and frame count without an application-selectable policy. | Commit `1695b7e6` adds bounded defaults and public custom limits for encoded bytes and frames. Byte rejection precedes scanning, and frame-limit rejection leaves iterator and reader state unchanged. The complete MP3 gate passes 6/6 steps and 131/131 tests, a further fuzz campaign passes 100,675 executions, and the installed package passes 18/18 steps and 96/96 tests. |
| Q-OGG-001 | Medium | Q10 | Closed | Ogg memory and positional-file readers bounded individual pages, packet storage, and resynchronization scans but accepted unbounded whole-stream bytes, page counts, packet counts, and chained logical-stream counts. | Commit `328adb46` adds bounded defaults and public custom limits across page readers, packet readers, and Vorbis seek-index helpers. Limit rejection preserves reader state, and determinable file limits precede caller-storage changes. The complete Vorbis/Ogg gate passes 10/10 steps and 94/94 tests, the native fuzz campaign passes 100,094 executions, and the installed package passes 18/18 steps and 96/96 tests. |
| Q-AUDIO-001 | Medium | Q12 | Closed | WAV, AIFF, AIFC, RF64, BW64, and Wave64 file readers validated individual chunk extents but accepted an unbounded whole file, chunk count, requested metadata chunk, and PCM frame count without an application-selectable policy. | Commit `4b326977` adds bounded defaults and public custom limits for file bytes, chunks per scan, requested metadata bytes, and frames. Determinable file limits precede parsing, accepted readers retain the policy, and metadata-limit rejection precedes caller-storage changes. The focused reader suite passes 18/18 tests, the broad Q12 selection passes 168/168 tests, the native fuzz campaign passes 100,153 executions, Windows ReleaseSafe compilation passes, and the installed package passes 18/18 steps and 96/96 tests. |
| Q-FLAC-001 | Medium | Q12 | Closed | FLAC memory and positional-file decoders bounded individual metadata and encoded frame operations but accepted unbounded whole streams, metadata totals, declared PCM frames, and decoded frame-block work without one application-selectable policy. | Commit `fa3e5f47` adds bounded defaults and public custom limits across memory and positional-file decoding. File metadata preflight rejects determinable limits before retained storage changes, accepted readers retain exact metadata totals and the selected policy, and transactional decode preserves PCM output when a later frame-block limit is reached. The focused suite passes 37/37 tests, the broad Q12 selection passes 169/169 tests, the native fuzz campaign passes 100,488 executions, Windows ReleaseSafe compilation passes, and the installed package passes 18/18 steps and 96/96 tests. |
| Q-META-001 | Medium | Q12 | Closed | Direct ID3v2.3, ID3v2.4, RIFF INFO, AIFF text, and RIFF XML parsing relied only on caller slice extent instead of an application-selectable byte policy. XML duplicate and expanded-name validation also performed pairwise attribute checks without an independent count or source-byte bound per element. | Commit `6551a601` adds 256 MiB bounded defaults and public custom policies to every direct metadata entry point, retains the selected policy and complete ID3 tag extent across iteration, rejects hostile extent resets, and caps each XML element at 1,024 attributes and 256 KiB of attribute source. The focused suite passes 41/41 tests, the broad Q12 selection passes 174/174 tests, the native fuzz campaign passes 100,017 executions, Windows ReleaseSafe compilation passes, and the installed package passes 18/18 steps and 96/96 tests. |
| Q-SOFA-001 | Medium | Q14 | Closed | The SOFA loader bounded every decoded NetCDF variable by compile-time measurement and frame capacities, but it passed an arbitrarily large container to the external NetCDF parser before those variable limits could apply. | Commit `604e6727` adds a 1 GiB bounded default and public caller-selected file-byte policy, checks the file before `nc_open`, and preserves failure-atomic destination publication. The exact HRTF gate passes 14/14 steps and 150/150 tests with both public datasets, independent NetCDF comparisons, and Linux plus Windows ReleaseSafe cross-builds. The installed package passes 18/18 steps and 96/96 tests, and the established libmysofa and libspatialaudio comparisons pass 6/6 steps. |
| Q-STATE-001 | Medium | Q07 | Closed | Parameter-state bytes had a fixed 16-bit entry-count bound and failure-atomic publication, but migration configuration was unbounded. Validation repeatedly scanned complete chains inside nested pair checks, and every decoded entry resolved its chain through repeated linear searches. | Commit `906296e3` accepts at most 256 migrations, proves the complete accepted chain and one-over rejection, replaces convergent-chain validation with the equivalent duplicate-destination invariant, and builds one fixed-storage sorted index before reading state. The broad state selection passes 198/198 tests, the native fuzz campaign reaches 200,041 cumulative executions, Windows ReleaseSafe compilation passes, and the installed package passes 18/18 steps and 96/96 tests. |
| Q-ARA-001 | Critical | Q02 | Closed | ARA store and restore filters validated counted-pointer shape but did not reject source or modification counts above the controller model capacities before traversing host arrays. A store filter containing repeated valid source references could therefore index beyond the fixed source-ID staging array. Restore filters also admitted work beyond the model bound. | Commit `7c392946` rejects all four counts before pointer traversal, adds exact one-over source and modification tests for store and restore, preserves the prior host archive on rejected stores, caps tuning-analysis archive fields to their 16-bit wire representation, and uses saturating archive-size arithmetic. The complete ARA gate passes 77/77 steps and 444/444 tests, two native fuzz campaigns pass 100,006 and 200,048 cumulative executions, TSan passes 10/10 steps and 17/17 tests, and the installed package passes 18/18 steps and 96/96 tests. |
| Q-MIDI-CI-001 | Medium | Q09 | Closed | Public MIDI-CI Property Exchange header JSON parsers accepted an arbitrary source slice before allocation and parsing even though the wire representation limits every header to a 14-bit byte count. Resource-body parsers had an explicit 65,535-byte policy, leaving the direct header API as the inconsistent unbounded entry point. | Commit `387204ef` enforces the 16,383-byte wire maximum at the shared header parser before allocation and exports the limit. Exact one-over and exhaustive allocation-failure tests pass. The 108-test focused gate and three dedicated fuzz campaigns covering wire parsing, reassembly, headers, Mcoded7, resource JSON, and cache restore pass across native and ReleaseSafe cross-target builds. The installed package passes 18/18 steps and 96/96 tests. |
| Q-EDITOR-001 | Medium | Q08 | Closed | Typed editor-state bytes, field counts, and payloads were fixed-capacity and restoration was failure-atomic, but the caller-provided migration table was unbounded. Validation compared every migration pair, and every decoded field rescanned the complete table once for every schema version. | Commit `88d286da` caps migrations at 256, validates and orders them once in fixed storage, and resolves each field in one pass over the index while preserving one migration per schema version. Exact-limit, one-over, ordering, duplicate, transactional, cross-target, installed-package, and 100,059-run fuzz evidence passes. |
| Q-RESOURCE-001 | Medium | Q07 | Closed | Resource reference decoding validated fixed path and metadata capacities, but the public `Recovery.restore` entry point accepted a directly constructed malformed reference without revalidation. It could replace a valid ready snapshot with invalid restoring state and submit work with a malformed path. | Commit `17eb9d39` validates the complete reference state before cancellation, generation changes, snapshot mutation, or worker submission. The negative control reproduced the ready-to-restoring transition. The regression proves exact snapshot preservation. A 134-test native and cross-target gate, 100,027-run state fuzz campaign, 60-test ThreadSanitizer gate, and installed-package matrix pass. |
| Q-ADAPTER-001 | Medium | Q04 | Closed | The VST3 data-exchange receiver formed a slice directly from a host-provided 32-bit block count and raw pointer. The interface does not declare a maximum count, so one callback could drive unbounded validation work before the framework delegate ran. | Commit `3a4f718e` caps each callback at 64 blocks and rejects zero or one-over counts before pointer traversal. Exact-limit and one-over regressions pass. The VST3 module gate passes 795/795 tests, the complete LV2 gate passes 56/56 steps and 578/578 tests, and the installed-package matrix passes 18/18 steps and 96/96 tests. |
| Q-CONFIG-001 | Low | Q01, Q06 | Closed | Device-selection restore bounded each field and published transactionally, but it lacked exhaustive truncation and exact whole-record evidence. The parser ledger also described a nonexistent standalone argument parser and classified numeric preparation configuration, a bounded event pump, and compile-time compatibility JSON output as runtime structured inputs. | Commit `5aa87178` checks all 656 truncated prefixes of a maximum-size record, invalid field encodings, trailing bytes, exact maximum size, and destination preservation. The dedicated gate passes 38/38 native tests plus ReleaseSafe Windows compilation. The inventory now retains only device-selection records and exact 32-byte textual VST3 IDs in P-CONFIG and records the three reviewed exclusions accurately. |
| Q-ARCH-001 | Medium | Q00, Q02, Q04, Q10–Q19 | Closed | The checked cohesion ledger classifies every large handwritten source. Selected ADM, MP3, ARA, VST3, Ogg, Vorbis, and other contracts were split behind exact public aliases when a narrower state boundary improved reviewability. | Commit `d1369e4e` completes the final split by separating the Vorbis codec from the Ogg container and compatibility facade. The cohesion inventory contains no `SPLIT` decisions, and focused, cross-target, independent-decoder, installed-consumer, parser, and inventory gates pass. |
| Q-ARCH-002 | Medium | Q14, Q15, Q16 | Closed | `dsp/hrtf.zig` imported the shared partitioned convolver from `gui_ir_convolution.zig`. The imported module owned DSP processing, lock-free preparation queues, realtime publication, and no GUI behavior required by HRTF. Its name and placement reversed the intended DSP-to-GUI dependency. | Commit `6ea6099b` moves the implementation to `dsp/convolution.zig`, makes HRTF depend within the DSP layer, and retains `gui_ir_convolution` as an exact public compatibility module. Focused convolution, HRTF, DSP ThreadSanitizer, IR Loader lifecycle and native plus Linux and Windows bundle, installed-package, inventory, and compatibility checks pass. |
| Q-ADM-001 | Medium | Q13 | Closed | `adm_xml.Document.init` accepted an unbounded byte slice, public XML iterators replayed their complete prefix before every item, and graph validation nested declaration, reference, cardinality, and block scans. | Commit `8f1d1750` adds exact retained-state witnesses with canonical fallback to XML event and attribute iteration. Commit `4c34d23f` adds explicit ADM byte, event, metadata-count, and graph-work limits; replaces the nested graph checks with exact fixed-storage indexes; and adds fuzz, limit, complexity, installed-consumer, and cross-target coverage. The pre-witness Debug negative control measured 115,542,863 ns per pair. The final 128/256/512-pair gate remains flat at 377,215/373,129/373,533 Debug ns per pair and 66,055/65,943/67,977 ReleaseSafe ns per pair. The complete 110-test ADM/XML selection, 100,668-run native fuzz campaign, and 96-test installed-package matrix pass. |
| Q-MIDI-001 | Medium | Q09 | Closed | `midi_file.TrackIterator.next` validated its retained cursor by replaying the track from byte zero before parsing the next event. A complete valid traversal therefore performed quadratic work in the event count, and `File.parse` did not impose a file, track, event, or work limit. | Commit `43b6c79b` carries a validated iterator witness, bounds fallback replay, and adds independent file, track, event, and payload limits. Debug and ReleaseSafe complexity gates remain flat from 1,000 through 4,000 events. Focused tests, 100,319 native fuzz executions, the installed-package suite, and Windows cross-compilation pass. |
| Q-MIDI-002 | Medium | Q09 | Closed | `midi_ump.Iterator.next` canonically traversed the complete source to validate its retained cursor before every packet. Valid UMP stream traversal was quadratic in packet count, and the iterator had no whole-stream word or packet policy. | Commit `d316b437` adds 256 MiB default and caller-selected word and packet limits, retains an exact source-and-state witness after complete validation, preserves bounded canonical fallback for caller-modified state, and publishes iterator progress transactionally. Exact-limit, one-over, cross-target, installed-package, 100,367-run mutation, and checked Debug and ReleaseSafe scaling evidence passes. |
| Q-MIDI-003 | High | Q18 | Closed | ALSA MIDI 1 and UMP input threads drained readable devices in inner loops that did not recheck the stop flag or impose a batch limit. A continuously readable device could keep `stopInput` waiting on its join indefinitely. The shared raw callbacks also trusted arbitrary byte and word counts before pointer traversal. | Commits `4e09e9e1` and `171a1218` cap callbacks at 256 MIDI 1 bytes or 64 UMP words before traversal, recheck stop during each native read, and cap each poll wake at 64 reads. CoreMIDI rejects counts beyond its 16-bit packet length, and direct C string copies reject addition overflow. Exact-limit and one-over regressions, ALSA MIDI, ALSA UMP, CoreMIDI, Windows UMP, ThreadSanitizer, cross-target, and installed-package gates pass. |
| Q-EDITOR-002 | Low | Q08 | Closed | The editor-state mutation helper used `catch unreachable` to construct a compile-time-valid seed. The helper was outside the lexical test block, so it violated the repository's zero-tolerance production termination-path gate even though the literal cannot fail. | Commit `d31a1ed2` uses compile-time error propagation instead. The termination-path scan and the editor-state mutation smoke gate pass. |
| Q-MEM-001 | Medium | Q04 | Closed | Framework VST3 component and controller objects hard-coded `page_allocator` for allocation and destruction. This preserved allocator compatibility in production but prevented deterministic failure injection for the owning object and processor-construction boundary. | Commit `8158b82d` stores allocator provenance, preserves the public ABI through allocator-injected internal creation, and adds outer and nested allocation-failure tests. The focused VST3 module gate passed 786/786 tests. |
| Q-INV-001 | Low | Q06, Q18 | Closed | The initial classifier assigned 29 native backend shim and scheduler support files to Q06 through the generic `plugin/*` fallback instead of Q18. All files were classified, but the ownership workload was attributed to the wrong review unit. | Commit `dd2ec5b2` adds explicit Q18 shim and scheduler rules before the Q06 fallback, adds a fixture assertion, and refreshes the checked inventory metrics. |
| Q-INV-002 | Low | Q00, Q18, Q19, Q21 | Closed | The source classifier did not recognize `.cc`, `.cxx`, or `.hpp`, and its atomic hotspot metric recognized Zig syntax but not C or C++ atomics. A new native header could therefore remain outside the review-unit inventory, while native synchronization density was materially understated. | Commit `71b7c997` classifies the additional native extensions, assigns the Windows UMP refcount header to Q18, recognizes C and C++ atomic syntax, and adds a fixture that requires both the header classification and native atomic count. The checked inventory covers 820 files. |
| Q-MEM-002 | Medium | Q06 | Closed | The runtime used the caller allocator correctly and had successful teardown coverage, but no exhaustive allocation-failure test exercised the plugin initializer transfer boundary. Running a focused runtime test also required the 1,792-test core group. | Commit `ca309fc7` adds exhaustive initializer-failure coverage and a focused build step. The gate passed 12/12 Debug tests, and the plugin-core ReleaseSafe cross-build passed. |
| Q-GUI-001 | High | Q03 | Closed | The LV2 VSTGUI backend registered accepted host peak subscriptions without unregistering them when editor construction failed or the editor was destroyed. This left stale host subscription state beyond the lifetime of the adapter that requested it. | Commit `02038b05` tracks each accepted subscription, rolls registrations back on construction failure, unregisters them before state destruction, and adds regression coverage. The VST3 module gate passed 787/787 tests and the editor lifecycle gate passed 2,384/2,384 tests. |
| Q-MEM-003 | Medium | Q17 | Closed | LV2 core instances, LV2 UI instances, and AUv2 component storage were allocated and destroyed with a hard-coded page allocator. The pairing was compatible in production but prevented deterministic outer-allocation failure and allocator-provenance tests. LV2 UI fallback cleanup also cleared registrations before editor-owned teardown could unregister them. | Commit `be81b415` stores allocator provenance in all three owning objects, injects failures at their outer allocation boundaries, verifies successful teardown through the testing allocator, and lets editor teardown release its registrations before fallback cleanup. The complete LV2 and Audio Unit gates passed. |
| Q-MEM-004 | Medium | Q18 | Closed | ALSA MIDI and Windows MIDI marked their backend open before the initial device query, then retained that state when the query failed. A caller could observe a failed open as successful, and a retry would silently skip initialization. | Commit `d28816e4` restores the closed state and zero topology on every failed initial query. Regression tests inject the query failure, verify rollback, and then open the same backend successfully. |
| Q-MEM-005 | High | Q18 | Closed | The PipeWire shim leaked a newly allocated properties object when common or target-specific property setup failed before `pw_stream_new_simple` accepted ownership. | Commit `d28816e4` loads `pw_properties_free` and frees the object on every pre-transfer failure. Successful calls preserve PipeWire's documented transfer of properties ownership to the stream constructor. The full native backend matrix passed with C undefined-behavior instrumentation. |
| Q-GUI-002 | High | Q19 | Closed | macOS accessibility elements and Windows UI Automation providers could outlive bridge teardown while retaining raw pointers to destroyed semantic nodes, views, or provider state. A later operating-system query could dereference released editor storage. | Commit `bd48cec4` explicitly detaches native accessibility objects before releasing bridge ownership. The macOS regression retains an element across editor close and verifies that all semantic access becomes inert. Native, cross-platform, ASan, and UBSan gates passed. |
| Q-MEM-006 | High | Q19 | Closed | VSTGUI editor construction could leave a partial frame owned outside RAII after a throwing or unsuccessful child build, an editable-label allocation failure leaked siblings allocated before parent transfer, and progress-control reopen replaced its retained timer without releasing it. Linux run-loop and AT-SPI registrations also crossed a fallible vector-growth boundary after foreign registration. | Commit `bd48cec4` gives incomplete frames one rollback path, rejects incomplete editors, frees pre-transfer label siblings, reuses the progress timer, reserves AT-SPI registration storage before publication, and rolls back run-loop registration if local tracking fails. Native tests, 24 ASan/UBSan process runs, four TSan runs, and 3,171 integration tests passed. |
| Q-MEM-007 | Medium | Q01 | Closed | Generic raw `PlugView` objects used `page_allocator` directly for creation and final COM release. Allocation and destruction matched in production, but the owner did not retain allocator provenance and its outer allocation could not receive deterministic failure injection. | Commit `22a2bd65` stores allocator provenance without changing the public factory, injects outer allocation failure, and releases a successful view through `std.testing.allocator`. The VST3 module gate passed 788/788 tests. |
| Q-MEM-008 | Medium | Q20 | Closed | Five product examples allocate processor engines at stable addresses through the caller allocator, but their public runtime initializers had successful teardown coverage without deterministic outer-allocation failure evidence. | Commit `53b2118a` adds a focused ownership gate and injects failure at all five outer allocation boundaries. The gate passed 44/44 tests and successful paths continue to tear down through `std.testing.allocator`. |
| Q-MEM-009 | Medium | Q18 | Closed | CoreMIDI retained the caller-borrowed input callback after both a failed source connection and a normal input stop. The stopped backend did not invoke the pointer, but its lifetime contract unnecessarily extended until restart or backend close. | Commit `e4910414` clears the callback after failed connection and after the callback gate drains on stop. Regression assertions cover both paths, and the complete focused MIDI matrix passed 60/60 tests. |
| Q-MEM-010 | High | Q02 | Closed | `openAudioReader` could call a host's `createAudioReaderForSource` when the same interface omitted `destroyAudioReader`. A non-null foreign reader then entered an owning slot with no guaranteed release callback, so later close leaked the host resource. | Commit `bbbfb882` requires the complete create, read, and destroy callback trio before acquisition and verifies that a missing destroy callback returns `MissingHostInterface` without calling create. The full ARA matrix passed 77/77 steps and 424/424 tests. |
| Q-CONC-001 | High | Q19 | Closed | The VSTGUI runtime guard used an atomic editor count without serializing the zero-to-one transition with `VSTGUI::init`. A second constructor could increment the count and return while the first constructor had not completed global initialization. The one-to-zero exit transition had the corresponding overlap risk. | Commit `23ad7a26` protects the count and both global runtime transitions with one mutex. The address and undefined-behavior sanitizer gate passed, and the TSan gate passed four process runs. |
| Q-CONC-002 | High | Q07, Q20 | Closed | Resource recovery invoked `Config.publicationReady` on its preparation worker. Model shell used that callback to dispatch a latency restart through `IComponentHandler::restartComponent`, which the pinned VST3 interface documentation requires in the UI-thread context. | Commit `83b88b6e` moves the callback to explicit control-thread polling, makes model-shell telemetry polling collect completion, and verifies that worker completion does not invoke the callback. Resource ownership passed 49 tests, owning examples passed 44 tests, and the resource TSan gate passed 57 tests. |
| Q-CONC-003 | High | Q18 | Closed | CoreAudio session and topology-observer teardown stopped the platform source and immediately freed callback contexts without an explicit admission barrier or in-flight drain. CoreMIDI input stop could return while a receive callback that had already observed the running flag continued to use callback and timebase state that `close` then cleared. | Commits `8674295f` and `a27291e0` added stop publication and callback draining. Commit `e4910414` subsequently replaced CoreMIDI's two-atomic admission scheme with the shared indivisible gate used by the other native MIDI backends. CoreAudio passed 10 tests with one hardware skip. The focused MIDI matrix passed 60 tests. |
| Q-CONC-004 | High | Q18 | Closed | ALSA MIDI, Windows MIDI, and the shared ALSA and Windows UMP backend used a running flag to reject callbacks but did not explicitly admit and drain callback work before clearing parser and callback-visible state. CoreMIDI's first drain used separate closing and active-count atomics, so closure and admission were not one indivisible state transition. | Commit `e4910414` introduces one atomic gate containing a closed bit and active count, applies it to all four backend families, and adds deterministic overlap tests. The focused matrix passed 45/45 build steps and 60/60 tests. Sixteen repeated aggregate TSan runs passed 64 processes and 112 selected tests. |
| Q-CONC-005 | Critical | Q02 | Closed | ARA audio-reader closure and read admission used separate `closing` and `active_reads` atomics. Close could publish closure, observe zero readers, destroy the foreign reader, clear non-atomic slot state, and reopen the flag before an acquiring thread incremented its count and performed its second flag check. That admitted read could then race slot mutation or call the destroyed host reader. | Commit `bbbfb882` combines the closed bit and lease count in one atomic word. Admission and closure now share one linearization point, close drains release-published leases with acquire order before destruction, and vacant slots remain closed. The focused ARA and TSan matrix passed 36/36 steps and 438/438 tests; 16 repeated Phase 2 TSan runs covered 48 processes and 224 selected tests. |
| Q-CONC-006 | Critical | Q18 | Closed | CoreAudio session and topology callback admission used separate `stopping` and `active_callbacks` atomics. A callback could observe the open flag, teardown could close admission and observe zero, then the callback could increment after the drain and access freed session or observer state. The second flag check did not close this interval. | Commit `4283284e` combines closure and the active count in one atomic word. A stale admission CAS now conflicts with closure, admitted callbacks release into the same word, and teardown drains the count before freeing callback-visible state. The native regression recreates the stale-observation ordering. The focused CoreAudio matrix passed 9/9 steps and 10/11 tests with one hardware skip, followed by 16/16 repeated focused runs. |
| Q-CONC-007 | Critical | Q01 | Closed | Dynamic COM reference counts saturated at `u32` maximum on `addRef` but decremented on the next `release`. Once saturation accepted an unrepresentable reference, later decrements made the stored count lower than the number of live holders and could eventually permit destruction while a holder remained. Static singleton counters had the same unstable saturation behavior without the destruction risk. | Commit `1f684081` makes maximum count a permanent pinned sentinel for dynamic and static release paths. Deterministic regressions verify stable saturation and prove dynamic release does not destroy a saturated object. The focused lifetime suite passed 16/16 tests, the VST3 module passed 791/791 tests, and commit `294ea628` adds the concurrent refcount tests to the aggregate Phase 2 ThreadSanitizer gate, which passed 10/10 steps and 16/16 tests. |
| Q-CONC-008 | Medium | Q16 | Closed | GUI editor activity saturated its atomic open count instead of wrapping, but the next close decremented the saturated sentinel. Opens accepted after saturation were not representable, so enough later closes could eventually report no active editor while one remained and disable editor-only processing prematurely. | Commit `e4b627a5` makes saturation a permanently active sentinel, records the publication contract, and adds deterministic saturation plus concurrent open-close regressions. The focused telemetry suite passed 19/19 tests, the VST3 module passed 791/791 tests, and the aggregate Phase 2 ThreadSanitizer gate passed 10/10 steps and 17/17 tests. |
| Q-CONC-009 | High | Q07 | Closed | `resource.Job.submit` reaped a completed worker before taking the state mutex. A worker could still appear running at that check, publish stopped before submit acquired the mutex, and leave its joinable handle in `thread`. Submit then spawned a replacement into the same field without joining the old handle, leaking native thread resources under a normal completion-submit interleaving. | Commit `1ef12711` keeps the worker running through result disposal and a final queued-work check, then joins any completed worker before replacing its handle. A deterministic regression holds the old worker between stopped publication and return and proves the replacement cannot start or return before the join completes. Resource ownership passed 51/51 tests, resource ThreadSanitizer passed 59/59 tests, and owning examples passed 46/46 tests. |
| Q-CONC-010 | Critical | Q18 | Closed | The Windows UMP receive callback used wrapping `fetch_add` and `fetch_sub` COM reference counts. Overflow could accept an unrepresentable reference, wrap the stored count, and later permit deletion while a holder remained. | Commit `71b7c997` uses a shared pinned-saturation counter and adds deterministic saturation plus 160,000 concurrent add-release pairs. The local ThreadSanitizer and Windows fallback cross-build gate passed. GitHub Actions run `31848681596`, Windows job `94920244792`, compiled and executed the native Windows SDK-backed UMP gate successfully. |
| Q-RT-001 | High | Q04, Q06 | Closed | `HostRequestSink` rejected `dispatchPending` from a realtime audit scope, but its three dynamic-topology mutation methods reached component locking without the same guard. The documented raw `SimpleEffect` snapshot, mutation, and host-dispatch APIs also bypassed the sink guards. Processor or integration code retaining either public surface could therefore block an audio thread or invoke a host callback from it. | Commits `244e1ae6`, `c4aeda04`, and `13b28faf` classify the sink and raw operations before component locking or host dispatch. Regressions verify rejection without mutation, pending-bit loss, or callback delivery and preserve control-thread behavior. The focused host-request tests passed 9/9, and the final VST3 module gate passed 7/7 steps and 788/788 tests. |
| Q-RT-002 | High | Q04 | Closed | Raw `SimpleEffect` channel-context, automation-state, and data-exchange queue lifecycle helpers delegated to host interfaces without enforcing their pinned UI-thread or main-thread contracts. Calling them from processing could execute unbounded host work. Data-exchange block lock and free have the opposite contract: the pinned interface requires them inside `IAudioProcessor::process`. | Commit `5b0f9271` rejects the four control-thread operations from realtime audit scopes before host invocation while preserving block lock and free as permitted realtime operations. The combined host regression records four rejected host calls with no delegation, then verifies successful control-thread calls and a clean realtime block lifecycle. The VST3 module gate passed 7/7 steps and 788/788 tests. |
| Q-RT-003 | High | Q07, Q16, Q20 | Closed | Public resource job, recovery, and decoded-audio importer control operations recorded a forbidden lock only inside their lock helpers and then blocked anyway. Recovery completion locking was not audited. Resource Swap advanced its generation before job submission rejected realtime allocation, and Fixed Rate could change latency and pending host-request state before dispatch rejection. | Commit `3491f3b9` rejects Debug and test realtime scopes at each public control boundary before locks, waits, input consumption, output writes, callbacks, generation changes, or control-state mutation. Valid inert snapshots and failure return values preserve the public contracts. Focused resource, importer, owning-example, VST3 module, and resource TSan gates pass. |
| Q-RT-004 | High | Q04 | Closed | VST3 accepted `setupProcessing.maxSamplesPerBlock` but did not retain or enforce it. A later host block larger than the prepared processor and scratch capacities reached processing, violating the negotiated work and storage bound. | Commit `10ea05e6` stores the maximum only after successful preparation and rejects negative or oversized blocks before collecting host input or invoking the processor. The regression preserves the successful in-bound block and verifies that an oversized block returns `kInvalidArgument` without touching output. The VST3 module gate passed 792/792 tests. |
| Q-RT-005 | High | Q04 | Closed | VST3 input storage capped accepted parameter changes and events at 64, but invalid queues, points, and events did not consume that capacity. A hostile host count containing invalid entries could therefore cause unbounded interface traversal on the processing thread. | Commit `10ea05e6` separately caps parameter queue visits, parameter point attempts, and event attempts at 64, including invalid entries. Regressions prove that each collector stops on its 64th invalid or empty attempt, and existing iterator tests prove host traversal stops when the collector returns false. The VST3 module gate passed 792/792 tests. |
| Q-VER-001 | Medium | Q18 | Closed | The ALSA UMP implementation was compiled as a named dependency of its public wrapper, so its implementation tests were not executed by `test-alsaump`. The wrapper's one import test passed while nine backend and unavailable-platform tests were compile-only. | Commit `e4910414` runs the implementation as a test root and retains the wrapper and cross-target checks. After the module-identity refinement in `42257ac2`, the step executes nine implementation tests plus two dedicated gate tests, and the full MIDI matrix passes. |
| Q-VER-002 | Medium | Q00, Q18 | Closed | The first shared MIDI callback gate was imported by relative path from four public backend modules. Each isolated module compiled, but a downstream consumer importing several backends assigned the shared file to multiple Zig modules and failed compilation. | Commit `42257ac2` gives the gate one named module identity across the public package graph and target-specific identities in isolated tests. The focused matrix passed 47/47 steps and 54/54 tests, and the installed-package consumer passed 18/18 steps and 96/96 tests. |
| Q-VER-003 | Medium | Q17 | Closed | The integrated LV2 UI lifecycle test reused cumulative fake-host and backend counters across an initial subscribe-and-cleanup probe and the main subscription scenario. Exact repository gates therefore found two observations where the later assertion expected one, while narrower public VST3 and LV2 gates did not execute the complete scenario directly. | Commit `719e82da` verifies and resets the initial host subscription pair. Commit `a79b294a` verifies and resets the matching backend creation count. The dedicated `test-lv2-ui-adapter` gate passes from fresh caches. |
| Q-VER-004 | Medium | Q00, Q20 | Closed | The realtime source audit listed 18 processor files by hand, omitted eight production processor files, and did not recognize the public parameter-view callback forms. Forbidden direct operations in those files or callback bodies could therefore escape the repository gate. | Commit `59bced5d` expands the audit to all 26 production processor files and all six public process entry forms. An exact inventory gate now rejects missing, stale, or duplicate paths, and its negative fixture proves that an omitted processor fails. The direct callback audit and checked quality inventories pass. Transitive helper review remains a separate Phase 2 task. |
| Q-VER-005 | Medium | Q00, Q18, Q19 | Closed | The atomic-order ledger tracked explicit Zig order tokens but omitted explicit C and C++ memory orders in native callback, shutdown, queue, reference-count, and GUI handoff code. Native order changes could therefore bypass the ledger gate. | Commit `a2e87365` adds checked relaxed, consume, acquire, release, acquire-release, and sequentially consistent counts for every native source in the concurrency inventory that uses an explicit order. Separate negative fixtures prove that changed Zig and native counts are both rejected. The gate tracks 57 Zig and 11 native files after the Windows UMP fix. |
| Q-VER-006 | Medium | Q00, Q19 | Closed | The VSTGUI ThreadSanitizer runner could complete all test processes, fail to write an iteration or final evidence file because the output volume was full, print the shell error, and still exit successfully. This allowed a missing-artifact run to appear green. | Commit `8f5f1154` checks every metadata, command, iteration, failure, and completion record write explicitly. Its runner fixture removes the active evidence directory after a successful fake test and requires a nonzero exit plus the precise artifact-write diagnostic. The runner self-test and a real four-process VSTGUI TSan run pass. |
| Q-VER-007 | Medium | Q00, Q20 | Closed | The checked realtime source inventory invoked `rg`, but the CI workflow did not install it. The current `macos-latest` image therefore rejected both the direct inventory and its negative fixture with exit code 127. | Commit `a79b294a` uses `git grep` over tracked Zig example paths. The baseline and both omission fixtures pass without an undeclared external search dependency. |
| Q-VER-008 | Medium | Q00, Q18 | Closed | The first Phase 2 completion candidate restored a macOS Zig cache and then seven native test artifacts terminated with `SIGSEGV`. The uncached replacement reproduced all seven failures on the new macOS 26 ARM `macos-latest` image. Every failed artifact used ThreadSanitizer, while the unsanitized tests passed and the same sanitizer gates pass on local macOS 15.4. | Commit `a79b294a` removed cache restoration as a possible cause, and commit `4466af3d` pinned every macOS CI job to the maintained `macos-15` ARM image. GitHub Actions run `31858188014` at that exact commit completed all 19 jobs successfully. The macOS build and test job passed the complete suite, and the separate macOS pluginval job passed normal and strictness-10 validation. |
| Q-VER-009 | Medium | Q00, Q09-Q13 | Closed | The aggregate `zig build test` gate was red because `check_production_termination_paths.sh` found ten termination constructs added after that zero-tolerance gate was introduced. Seven were unreachable fallbacks in top-level parser fuzz helpers, one was a compile-time ADM assertion, and two Ogg parser paths relied on `orelse unreachable`. | Commit `91a7e2e4` gives every fuzz selector a bounded fallback, replaces the ADM assertion with a compile-time diagnostic, and handles nullable Ogg page state explicitly. The production scan and its negative fixture pass. Every affected parser gate passes, and the complete repository gate passes 446/446 steps with 7,556 tests passed and six expected platform skips. |
| Q-VER-010 | Medium | Q00, Q18 | Closed | The ALSA MIDI 1 and UMP stop-loop refinement added one acquire load to each native shim, but the checked atomic-order ledger retained the previous counts. The first Phase 3 completion gate correctly rejected both stale entries. | Commit `02e0a43c` refreshes the two exact acquire counts. The ledger and its mutation fixture pass, and the repeated complete repository gate passes 446/446 steps with 7,647 tests passed and six expected platform skips. |
| Q-PERF-001 | Medium | Q15 | Closed | The least-squares FIR benchmark repeatably exceeded its 100 ms setup budget at 110.5–111.4 ms. Every grid row evaluated each cosine basis term with a separate transcendental call. | Commit `f916c524` evaluates one cosine per grid row and derives the remaining basis with the bounded Chebyshev recurrence. The SciPy 1.17 response comparison, 39-test numerical suite, and maximum-tap finite test pass. The unchanged benchmark workload now measures 90.7 ms, an 18 percent reduction from the 110.5 ms repeat run, and the complete benchmark gate passes. |
| Q-VER-011 | Medium | Q00, Q15 | Closed | The contended realtime snapshot benchmark treated `RealtimeSnapshotUnavailable` as fatal even though bounded publication explicitly permits that transient result when inactive slots are occupied. A valid non-blocking outcome therefore prevented the benchmark gate from completing. | Commit `6ff1533c` measures a fixed number of bounded attempts, counts expected unavailable results, and requires at least one successful publication plus concurrent reader progress. The 18-test snapshot suite passes. The benchmark completed 19,903 successful publications in 20,000 attempts at 40.2 ns per attempt and passed every regression budget. |
| Q-VER-012 | Medium | Q00, Q10-Q15 | Closed | The quality classifier assigned new Phase 4 submodules under Ogg, MP3, ADM, HRTF, and matrix directories to generic Q15 instead of the review units that own their facades. Later unit-scoped audits could therefore omit implementations while appearing complete. | Commit `5930f9fa` assigns every extracted directory to the same Q10–Q14 unit as its facade and adds exact fixture counts for all five directory families. The fixture rejects an unclassified source, the complete inventory passes with 866 files and 478,499 lines, and the recorded unit and lexical metrics are refreshed from that output. |
| Q-VER-013 | Medium | Q00, Q15 | Closed | The Phase 5 benchmark printed representative sample and IR fixed-storage sizes but did not enforce memory regression ceilings. Its evidence record also described the 63-tap least-squares workload as 255 taps, so the required explicit-input record disagreed with the executable gate. | Commit `330d0c3a` enforces five fixed-storage ceilings at the benchmarked sample and IR capacities. The corrected evidence records the actual 63-tap, two-band, 1,024-density least-squares input and the complete setup, realtime, memory, and hostile-scaling matrix. The bounded one-job benchmark passes 5/5 steps and every threshold. |
| Q-VER-014 | Medium | Q00, Q01, Q02 | Closed | The 33 VST3 and ARA ABI scripts wrote generated comparators into the repository `.zig-cache` even when the caller supplied another local cache. Nested Zig commands also fell back to the user-wide global cache, and the ARA checker explicitly overrode both configured roots. A bounded verification run could therefore escape its declared storage roots, fail under restricted cache access, and leave artifacts that were not reclaimed with the requested cache. | Commit `af412dda` propagates the build graph's local and global cache roots to script checks, routes every ABI output through `ZIG_LOCAL_CACHE_DIR`, and removes the ARA overrides. A fixture checks all 33 scripts and executes a representative checker with fake compilers to prove output and nested Zig environment routing. The corrected raw ABI matrix passes 135/135 steps with all 33 output directories under the supplied temporary local cache. |
| Q-MEM-011 | Critical | Q15 | Closed | Mutable audio blocks accepted overlapping channel storage, and copy plus arithmetic operations accepted shifted and cross-channel source aliases. `copyFrom` could therefore invoke `@memcpy` with overlapping regions, violating the builtin memory contract, while arithmetic could silently consume values already overwritten earlier in the same operation. | Commit `97657397` requires disjoint mutable channels, rejects every shifted and cross-channel source alias before mutation, permits exact corresponding in-place sources, and treats hostile retained overlap as invalid state. Debug and ReleaseSafe focused tests pass 9/9, including transactional shifted and swapped-channel regressions. ReleaseSafe installed-package consumers pass 18/18 steps and 96/96 tests. |
| Q-MEM-012 | High | Q15 | Closed | Ballistics, delay, first-order TPT, ladder, lookup-table, and state-variable block processing accepted shifted input and output slices. Earlier output writes could therefore replace samples that a later iteration had not consumed, producing order-dependent output and advancing processor state from corrupted input. | Commit `23e93442` centralizes checked slice-region classification, permits exact in-place processing, and rejects every other overlap before output or state changes. Debug and ReleaseSafe focused gates each pass 41/41 tests, including shifted-overlap transactionality for all six processor families. |
| Q-NUM-001 | Medium | Q15 | Closed | Window application accepted finite samples but did not validate the derived normalized products. A coefficient above one could turn a finite extreme sample into infinity, and detecting a later failure would have left earlier samples transformed. Generated window values also lacked an explicit finite-output check. | Commit `87b80e0b` preflights every generated coefficient and applied product, rejects non-finite normalization scales, and leaves caller storage unchanged on failure. Debug and ReleaseSafe direct gates each pass 33/33 tests, including the extreme finite transactional regression and independent Kaiser special-function vectors. |
| Q-NUM-002 | Medium | Q15 | Closed | Dry/wet mixing validated both input streams and produced bounded finite gains, but did not validate their weighted sum. Equal-power mixing of two extreme same-sign finite samples could produce infinity, and block mutation was not transactional if a later frame overflowed. | Commit `01a3c774` preflights every mixed output before mutation and preserves the wet buffer plus pending dry block on failure. Debug and ReleaseSafe direct gates each pass 4/4 tests, including all eight gain laws, integer and fractional latency, sequencing failures, and the extreme finite regression. |
| Q-NUM-003 | High | Q15 | Closed | Fast exponential and tangent accepted domains much wider than their checked evidence. The f32 exponential Padé had more than 100 percent relative error at -6, and the tangent Padé's approximate pole caused severe error arbitrarily close to the accepted exact ±π/2 boundaries. | Commit `323a977f` adds base-2 range reduction to scalar and SIMD exponential evaluation and uses the standard tangent only in the narrow pole boundary while retaining Padé evaluation elsewhere. Full-domain f32 and f64 reference sweeps, scalar and SIMD parity, transactionality, and Debug plus ReleaseSafe gates pass 18/18 tests. |
| Q-MEM-013 | High | Q15 | Closed | Processor chains, independent processor duplicators, and shared-state processor duplicators accepted shifted input and output slices. Their forward block loops could overwrite unconsumed samples and advance per-processor state from corrupted input. | Commit `8f55fbbf` applies the shared exact-or-disjoint slice contract to all three composition adapters before processing. Debug and ReleaseSafe focused gates each pass 19/19 tests, including unchanged output and processor state after shifted-overlap rejection plus exact in-place behavior. |
| Q-MEM-014 | High | Q15 | Closed | Stereo panning accepted overlapping left and right destinations plus shifted aliases between input and either output. One output plane could overwrite the other, or an earlier write could replace input that a later frame had not consumed. | Commit `1d6e62d0` requires disjoint output planes and permits only exact input aliasing with one output plane. Debug and ReleaseSafe direct gates each pass 9/9 tests, including unchanged destinations after shifted and same-output rejection plus exact in-place processing. |
| Q-MEM-015 | High | Q15 | Closed | Chorus, flanger, modulated delay, phaser, and vibrato accepted shifted mono aliases. Reverb accepted overlapping stereo destinations and shifted input aliases. Stereo modulation also allowed cross-channel aliases even though it completes the left processor before the right processor reads its input. | Commit `9b4ec47a` applies exact-or-disjoint mono contracts, disjoint reverb outputs with per-frame-safe exact input replacement, and stricter corresponding-only replacement for sequential stereo modulation. Wrapper-specific overlap errors remain distinct from length failures. Debug and ReleaseSafe composite gates each pass 41/41 tests with unchanged output and state on rejection. |

No critical or high finding is open at the start of Phase 0. This statement is
an inventory status, not a conclusion about code that has not yet received its
assigned review.
