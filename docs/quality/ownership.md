# Quality Ownership Record

This record names allocation provenance, transfer, teardown, and failure
evidence for owning types reviewed in Phase 1. A row covers only the stated
type and boundary.

## Q04: VST3 Framework Adapters

| Owner | Acquired resources | Transfer and borrows | Teardown contract | Failure evidence |
| --- | --- | --- | --- | --- |
| Reflected edit `Controller` | Its own allocation; optional controller state; retained component peer, telemetry source, component handlers, unit handlers, progress interface, and host application | VST3 interface queries retain the same controlling object. Returned editor and controller-state pointers borrow the controller lifetime. | The final COM release deinitializes optional state, releases every retained interface, then destroys the object with its stored allocator. `terminate` releases host-facing references early, and final release tolerates the cleared slots. | `reflected edit controller reports outer allocation failure` verifies a null output and no retained allocation when object creation fails. Existing replacement and lifecycle tests verify balanced interface retains. |
| Simple effect `Component` | Its own allocation; processor runtime allocations; embedded ARA extension and entry point; retained connection peer, host application, channel listener, automation state, and data-exchange handler | Delegated VST3, telemetry, and ARA identities retain the component. The processor, ARA extension, host-request sink, and bus snapshots are embedded and cannot outlive it. | The final COM release unbinds and deinitializes ARA, deinitializes the processor, releases retained interfaces, then destroys the object with its stored allocator. Processor construction receives that same allocator. | `simple effect cleans up allocation failures with owning allocator` injects failure at outer-object and processor-storage allocation, then verifies successful final release through the same allocator. The existing fallible-construction test verifies null output on a plugin-defined initialization error. |
| Runtime adapter `ProcessorWithParameters` | `ProcessorRuntime`, the plugin instance, reflected parameter storage, and plugin-owned resources allocated through the caller allocator | Resource and import receiver pointers borrow the embedded plugin lifetime. The host-request sink is borrowed from the containing component and must be unbound or become unreachable before component destruction. | `deinit` delegates to `ProcessorRuntime.deinit`. Deactivation releases prepared resources while preserving an initialized runtime that may be prepared again. | Runtime-adapter tests use `std.testing.allocator` for initialization and deinitialization. The enclosing component failure test now reaches allocator failure inside processor construction. |

### Q04 callback context rule

The component address is stable from successful allocation until the final COM
release. Every callback context that points into the component is either an
embedded interface whose query increments the component reference count or a
borrow passed to the embedded processor. Final release deinitializes the
processor before destroying the component allocation. A retained external
interface is released before destruction, and all release helpers clear their
slots so `terminate` followed by final release is idempotent.

## Q06: Framework Runtime Core

| Owner | Acquired resources | Transfer and borrows | Teardown contract | Failure evidence |
| --- | --- | --- | --- | --- |
| `PluginInstance` | The concrete plugin value returned by its optional `init(Allocator)` hook | Reflected parameter and unit declarations are compile-time values. Views into parameter state and plugin fields borrow the instance lifetime. | If initialization succeeds, the instance exclusively owns the plugin value and invokes its optional `deinit` hook exactly once through its runtime owner. A failing plugin initializer never transfers a value to the instance. | Lifecycle validation requires the allocator-taking fallible initializer and matching pointer-based `deinit` signature. Runtime tests use the Debug allocator for plugin-owned storage. |
| `ProcessorRuntime` | One `PluginInstance` and its prepared plugin resources | Process contexts, host request sinks, and state readers or writers are call-scoped borrows. The runtime does not retain their slices. | `deinit` is idempotent. It deactivates an active instance, releases prepared resources, enters the terminal state, then deinitializes the plugin. Reinitialization requires a new successful `initInto`. | `processor runtime propagates allocation failure without ownership` exhaustively injects allocation failure into a representative plugin initializer. Existing terminal-state tests verify one successful allocation is released and repeated teardown is harmless. |
| `OfflineRenderer` | One `ProcessorRuntime`; all block headers and event staging are fixed stack storage | Render option slices and the optional output event writer borrow the caller for the duration of `render`. | Failure after prepare invokes state-aware cleanup. Normal and error exits deactivate and release prepared resources; `deinit` delegates final plugin ownership release to the runtime. | Existing tests cover invalid options, processing failure cleanup, repeated render, output capacity, and final Debug allocator teardown. |

## Q07: State and Resource Ownership

| Owner | Acquired resources | Transfer and borrows | Teardown contract | Failure evidence |
| --- | --- | --- | --- | --- |
| `ByteAccumulator` | One growable byte allocation from its stored allocator | `bytes` borrows until mutation or teardown. Successful `toOwnedSlice` transfers the allocation to the caller, which must free nonempty storage with the same allocator; an empty result has no allocation. | `deinit` frees retained storage and clears the writer state. A failed grow or shrink retains the original allocation and logical bytes. | Debug tests cover initial allocation, growth, reallocation failure, limit rejection, alias rejection, ownership transfer, malformed internal state, and writer arithmetic. |
| Resource `Job` | One optional worker thread, a copied queued request, one copied retry request, and at most one completed `Result` | `Config.run` transfers a successful result to the job. `takeResult` transfers it to the caller. Superseded, rejected, abandoned, or untaken results pass exactly once to `Config.dispose`. Request values must be safe to copy and remain valid through worker completion. | `deinit` prevents new work, invalidates queued and running generations, removes and later disposes the stored result, joins the worker, and relies on stale-worker completion to dispose any concurrently produced result. | Tests cover replacement disposal, cancellation, reset while running, deadline and work limits, result limits, generation wrap, worker failure, retry, and teardown. |
| Resource `Exchange` | Pointers transferred into fixed publication slots | Successful `publish` transfers the resource to the exchange. Any publish error leaves ownership with the caller. `View` and `MutableView` borrow only while their slot remains active and processing has not crossed a retirement boundary. | Retired slots are destroyed exactly once by non-realtime `reclaim`. After processing stops, `retireAllAfterProcessingStops` followed by `deinit` destroys pending and active resources. | Tests cover replacement, busy and invalid publication, malformed slots, stale generations, wraparound, mutable activation, reclamation, teardown, and concurrent publication. |
| Resource `Recovery` | One `Job`, one `Exchange`, bounded source and reference values, and prepared resources returned by `Config.prepare` | A prepared resource remains worker-owned until successful exchange publication. Cancellation, validation failure, stale completion, changed identity, unsupported schema, or busy publication invokes `Config.destroy`. Successful publication transfers ownership to the exchange. | `deinit` first cancels and joins preparation, then retires and destroys every exchange resource. Active resource pointers borrow until the documented block-boundary retirement. | Recovery tests cover import, restore, relink, cancellation, replacement, large bounded resources, publication context, invalid prepared data, and teardown. The focused ownership gate runs all matching cases with the Debug allocator. |
| Parameter, unit, reference, and migration values | Fixed-capacity or compile-time storage; no heap ownership in Q07 | Returned slices and views borrow their containing value. Decoded state is built as a local value before caller publication. | Value teardown requires no allocator. Resource references bound paths and metadata in the type. | State and reference tests cover truncation, malformed lengths and tags, trailing bytes, migration failure, and transactional decode. |

## Q02: ARA Ownership

| Owner | Acquired resources | Transfer and borrows | Teardown contract | Failure evidence |
| --- | --- | --- | --- | --- |
| ARA `Factory` | A static fixed-capacity pool of controller and optional extension slots | Successful creation lends the host an ARA controller instance backed by one occupied slot. The copied host interface table and every host reference remain host-owned and must outlive the controller. | Host destruction closes controller readers, marks the controller destroyed, invokes the slot release callback, detaches the extension, and returns the slot to the pool. Factory uninitialization rejects live, attaching, or detaching slots. | Factory tests cover exhaustion, extension attach rollback, detach failure, reentrant active-count observation, slot reuse, active-controller rejection, and initialization state. |
| ARA document `Controller` | Bounded inline document model, archive buffer, audio-reader slots, content-reader slots, observer slots, and copied host interface pointers | The caller or factory owns the controller storage. Model sinks and content-provider callback contexts are unretained borrows and must be removed or remain alive until controller destruction. Returned object and reader references encode bounded slot generations. | `destroyDocumentController` drains audio reads before invoking each host reader destructor, clears content readers, closes editing, marks the controller unusable, then invokes the optional storage-release callback as its final action. | Native tests cover reader exhaustion, concurrent close and read leases, stale references, repeated destroy calls, archive failures, observer replacement, host callback validation, and bounded model teardown. |
| ARA source `Cache` and `PagedCache` | Fixed inline sample pages and immutable publication slots; no heap allocation | Renderer providers borrow the cache at a stable address. Acquired publication handles pin a generation only through the callback and release before return. Host audio readers are call-scoped and close on every load exit. | The cache has value lifetime only. Its provider must be detached before moving or destroying the cache. Failed fills cancel the unpublished slot and preserve the active generation. | Dedicated source-cache tests cover failed and partial reads, overlapping outputs, stale generations, hostile ranges, publication exhaustion, concurrent reads, eviction, refresh failure, and ReleaseSafe cross-builds. |
| ARA extension and render state | Fixed-capacity reference sets, render plans, analysis buffers, and static VST3 main-factory registration | The extension borrows its document controller and publication sink context for the entire binding. Playback and editor references remain host-owned opaque identities. | Extension state becomes unreachable with the containing component. Factory registration is static and COM queries retain its static controlling identity without dynamic allocation. | Native ARA tests cover role validation, assignment capacity, transactional selection updates, stale model references, render invalidation, and main-factory identity. |

## Q03: Raw VSTGUI and Wayland Ownership

| Owner | Acquired resources | Transfer and borrows | Teardown contract | Failure evidence |
| --- | --- | --- | --- | --- |
| VST3 VSTGUI `IPlugView` binding | Page-allocated binding and telemetry state, one native editor, one retained edit controller, one optional retained telemetry source, and one controller observer registration | Native creation arguments are call-scoped. Resize callbacks borrow the view until native editor destruction. The Wayland host pointer and controller observer context borrow their containing component, which outlives the retained view. | Final COM release closes telemetry if attached, unregisters the observer, destroys the native editor, releases the controller and telemetry source, then destroys both page allocations. Every construction exit unwinds in reverse acquisition order. | The VST3 module tests cover observer registration failure, unsupported platforms, native creation failure, attachment, removal, telemetry transitions, and final release. The complete editor lifecycle gate exercises every example editor. |
| LV2 VSTGUI backend state | One page allocation, one native editor, active gesture state, and accepted host peak subscriptions | The stored GUI context is a borrow from the enclosing LV2 UI instance. Native callbacks borrow the stable backend allocation. Peak measurements are copied into fixed inline storage. | Editor teardown closes attachment, finishes the active gesture, unregisters every accepted peak subscription, destroys the native editor, then frees the state. Construction failure runs the same subscription rollback before freeing state. | `VSTGUI LV2 backend owns native lifecycle and host callbacks` verifies normal unsubscription. `VSTGUI LV2 backend rolls back host peak subscriptions` verifies failure unwind after an accepted registration. |
| Standalone Wayland bridge | No heap allocation. It stores borrowed window and run-loop pointers, a shared atomic COM reference count, and a counted Wayland display borrow. | Interface queries share one controlling identity. A queried run loop is retained independently. Each successful display open creates one counted borrow that requires a matching close. | The stack owner must call `validateDetached` before destroying the bridge or its borrowed window. Validation rejects outstanding display borrows and interface references. | Tests cover canonical interface identity, balanced queries, connection overflow, foreign and closed displays, resize validation, and detached-state rejection. |
| Raw Wayland wrappers and headless host | Stack-owned interface wrappers and temporary queried COM interfaces | Platform handles remain host-owned. Headless views and queried component, processor, controller, and connection-point interfaces are retained only within their lexical scope. | Every successful query has a matching deferred release. Wrapper reference counts do not destroy caller-owned stack storage. | Raw Wayland tests cover query output clearing, shared reference counts, pointer validation, and delegated hooks. Headless lifecycle runs validate temporary interface release across all example editors. |

## Q17: LV2 and AUv2 Host Adapter Ownership

| Owner | Acquired resources | Transfer and borrows | Teardown contract | Failure evidence |
| --- | --- | --- | --- | --- |
| LV2 core instance | One allocator-owned instance, one processor runtime, optional plugin bindings into embedded sink fields, and borrowed host feature callbacks | Port buffers are host-owned call-to-call borrows. Feature structs and callback handles remain host-owned for the instance lifetime. Plugin sink pointers target stable fields in the allocated instance. | Host cleanup deinitializes the runtime before destroying the instance with its stored allocator. Every failed feature validation, URID mapping, option read, or runtime preparation deinitializes the runtime before freeing the instance. | A failing allocator rejects the outer instance allocation. A successful logging instance is created and cleaned up through `std.testing.allocator`. Existing tests cover malformed and missing features, failed mapping, preparation, activation, worker state, and cleanup. |
| LV2 UI instance | One allocator-owned instance, one backend editor, fixed registration tables, and borrowed host callback features | The editor context borrows the instance. Host widget, controller, feature, and callback handles remain host-owned. Accepted peak and atom registrations are tracked in fixed storage. | Editor teardown runs first so backend-owned registrations can be removed through the live context. Wrapper cleanup then releases any registrations left by a backend and destroys the instance with its stored allocator. Construction failures follow the same order once an editor exists. | Tests inject outer allocation failure, destroy a successful instance through `std.testing.allocator`, and verify an editor-owned dynamic subscription receives an accepted unsubscription during teardown. Host fixtures exercise loaded-library cleanup. |
| AUv2 component storage and render adapter | One allocator-owned storage block containing the render adapter, property adapter, and dispatch interface. The render adapter owns one processor runtime. | Audio component instance handles, render callbacks, property listeners, and render notifications are host-owned borrows stored until removal or close. Buffer lists and timestamps are call-scoped. | Component close uninitializes processing, clears callback tuples, invokes the one-shot storage close callback, deinitializes the render adapter, and destroys storage with its stored allocator. A failed render-adapter initialization frees the outer storage immediately. | The factory test injects outer allocation failure and closes a successful component through `std.testing.allocator`. Audio Unit tests cover repeated open and close, initialization failure, listener removal, render notification mutation, malformed buffers, and host fixture teardown. |
| AUv2 class-info bridge | Core Foundation data, string, number, and dictionary objects allocated by the native bridge | The returned dictionary is transferred to the Audio Unit host as the class-info property value. The dictionary retains its entries. Input state and name bytes are copied during creation. | Partial construction releases every non-null Core Foundation object. Successful construction releases temporary entries after dictionary creation; the receiving host owns the returned dictionary reference. | Native tests validate class-info round trips and malformed values. The C bridge checks null pointers and `CFIndex` bounds before allocation or copy. |
| Audio Unit render core and LV2 metadata | Processor runtime ownership inside value storage; metadata output is written to caller-provided writers | Render options, channel slices, state readers and writers, and metadata writers are call-scoped borrows. No additional outer heap object is acquired. | Render-core `deinit` delegates to processor runtime teardown. Metadata generation owns no storage beyond its call. | Audio Unit Debug tests cover initialization and state failures, rendering cleanup, input callbacks, scheduled events, and final runtime teardown. Metadata output is exercised by bundle generation and validation. |

## Q18: Native Audio, MIDI, and Window Ownership

| Owner | Acquired resources | Transfer and borrows | Teardown contract | Failure evidence |
| --- | --- | --- | --- | --- |
| CoreAudio, WASAPI, ALSA, and PipeWire audio backends | Native audio sessions, device catalogs, topology observers, dynamic-library state where required, and callback state stored inline in the Zig backend | Process callbacks and device selections borrow caller storage only for their documented call or active-session lifetime. Native callback contexts point to a stable backend value while the session is running. PipeWire properties transfer to `pw_stream_new_simple` when that function is called. | `stop` first stops or joins the native session, then clears callback borrows. `deinit` stops audio before closing topology observation. Partial native construction converges on the shim cleanup path. PipeWire now frees properties on every failure before stream ownership transfer. | Mock backends cover start failure, callback failure, recovery, topology changes, and repeated stop. Native availability checks and ReleaseSafe cross-target link fixtures cover each platform branch. The focused native matrix compiles host shims with full C undefined-behavior instrumentation. |
| ALSA, Windows, and CoreMIDI byte-stream backends | Native input and output handles, scheduler queues, device catalogs, input threads, and CoreMIDI client and port objects | Input callbacks borrow their context while input is active. Scheduled messages are copied into bounded queues. Device descriptors are copied into caller storage. | Input stop joins or drains the input path before callback state is cleared. Output closes before shared native state is released. CoreMIDI disposes output, input, and client objects in reverse dependency order. Failed ALSA and Windows initialization now restores the fully closed state. | Tests cover device-query failure and retry, input and output open failure, queue bounds, rejection, topology polling, output close, and scheduler ordering. Native and cross-target link checks cover all three platform families. |
| ALSA and Windows UMP backends | Native endpoint handles, bounded packet queues, scheduler state, and input workers | Callback contexts borrow the backend during active input. UMP packets and scheduled timestamps are copied into bounded native queues. | Stop prevents new delivery, joins the input worker, closes output, and releases scheduler synchronization objects before freeing native state. Repeated stop and close are harmless. | Focused tests cover packet validation, queue ordering, unavailable-platform behavior, native compilation, and ReleaseSafe link fixtures. |
| Standalone Win32, Cocoa, X11, and Wayland windows | One native window allocation and platform handle, plus backend-specific dynamic symbols or framework objects | The native parent handle remains host-owned. Editor callbacks borrow the stable standalone window while attached. | Standalone editor teardown closes and detaches the editor before destroying the native window. Failed parent lookup or partial native creation destroys any acquired window state. Close is idempotent. | Native tests cover invalid dimensions, create and close, unsupported platforms, and repeated teardown. Link fixtures exercise native and cross-target platform branches with full C undefined-behavior instrumentation on the host shim. |

## Q19: VSTGUI C++ and Native Platform Ownership

| Owner | Acquired resources | Transfer and borrows | Teardown contract | Failure evidence |
| --- | --- | --- | --- | --- |
| `ZigVstguiEditor` | One C++ editor allocation, copied descriptions and strings, owned control adapters, one retained update timer, one VSTGUI frame tree, assets, and optional retained host platform interfaces | Creation copies every variable-length description needed after the call. Callback tables and their userdata remain caller-owned and must outlive the editor. A native parent is borrowed only while the frame is open. | Close stops every timer and dynamic control, closes accessibility, clears listeners, prepares the platform frame, and closes it before clearing raw view references. Destruction also releases an unopened frame and retained host interfaces. Partial frame construction now uses the same listener and frame rollback. | Native creation and destruction tests cover malformed descriptions and repeated open and close. Every child build must produce its expected focus view before the frame is marked complete. The complete example lifecycle gate exercises 3,171 editor-integrated tests. |
| Control adapters and VSTGUI view tree | Control adapters own timers, copied model state, and callbacks. Once `addView` succeeds, the parent VSTGUI container owns the view reference; adapters retain only raw observer pointers. | View listeners and accessibility action handlers borrow their adapter. Drawing canvases, file packages, and callback request data are call-scoped. Assets own copied bytes and VSTGUI bitmap references. | `clearControls` unregisters listeners and action handlers before the frame releases child views. Timers stop before their referenced views disappear and are either retained for a later reopen or forgotten exactly once. Pre-transfer editable-label siblings are forgotten on partial allocation failure. | Adapter tests exercise controls, files, menus, gestures, timers, assets, drawing, and repeated reopen. ASan and UBSan ran adapter and visual suites eight times each. |
| Native accessibility bridges | Linux D-Bus registrations and object models, macOS accessibility elements and observers, Windows COM providers and subclass state, and platform clipboard resources | Semantic nodes and VSTGUI views are borrowed only while the bridge is active. Native clients may retain an accessibility element or COM provider beyond bridge close, so retained objects must become inert before borrowed state is destroyed. | Close clears semantic observers first. Linux unregisters D-Bus objects in reverse order. macOS clears associated semantic and view pointers on every element. Windows clears provider state before releasing bridge-held COM references and removes the window subclass. | The macOS test retains an element across editor close and queries it safely after detachment. Linux AT-SPI and clipboard fixtures exercise registration, teardown, protocol errors, and bounded transfers. Windows code receives cross-compilation. |
| Linux VSTGUI platform bridge | Retained host run-loop, Wayland host, and Wayland frame interfaces plus registered event-handler and timer adapters | The VSTGUI handlers are borrowed while their tracked host registration is active. Queried VST3 interfaces are retained for the wrapper lifetime. | Destruction unregisters every tracked handler before releasing its adapter and then releases the host run loop. Failed host registration releases the new adapter. Failed local tracking now unregisters the accepted foreign registration before release. | Native platform tests and Linux cross-compilation cover X11 and Wayland configuration. TSan ran the adapter concurrency suite four times. |
