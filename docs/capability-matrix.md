# Framework Capability Matrix

This matrix tracks `zig-vst3` against plugin product requirements, public format specifications, platform SDKs, and independently testable DSP behavior. It does not prescribe API shape or architecture.

## Summary

For VST3 plugin authoring, `zig-vst3-plugin` already covers the core processor lifecycle, 32-bit and 64-bit audio, sample-accurate automation, input and output events, state, units and programs, latency and tail reporting, resources, and an optional VSTGUI editor path. It also has unusually strong bounded realtime data structures and automated VST3 validation.

ARA host-contract fixtures cover phase-safe controller reservation, reentrant product extension hooks, extension failure rollback, generation-checked observer publication, two-phase archive ownership, legacy restore, corrupt-input rollback, and retry after failed decoding. External ARA-host behavior remains unconfirmed.

Standalone lifecycle fixtures cover callback drain during stop, stale-callback silence after stop or failed startup, processor rollback, restart, and retry. Physical device timing and automatic recovery remain unconfirmed.

LV2 component state supports host-mapped external resources and host-created generated paths through scoped, explicitly released path values. Typed properties support sample-timed Patch Set and property-specific Get messages with bounded responses. Generated project, license, maintainer, preset, Programs, and UI metadata passes independent schema validation and warning-fatal direct-distribution lint. General Patch graph operations and external-host state portability remain open.

The largest remaining plugin-authoring gaps are:

1. AUv3 and AAX wrappers, plus remaining LV2 extensions and external-host coverage for LV2 and AUv2.
2. Physical validation of the native Windows MIDI Services and PipeWire backends.
3. Physical standalone window, Linux VSTGUI run-loop, and automatic-recovery validation.
4. Validate the implemented tuning, tempo, note, meter, key, chord, fade, warp, spectral transformation, persistence, and playback paths in external ARA hosts.
5. A broader reusable DSP and audio-file library.
6. Real-host confirmation for capacity-selected high-count bus configurations. Automated VST3, LV2, AUv2, standalone, offline, and native-device backend fixtures cover 12 auxiliaries per direction.

Plugin hosting, broad device management, mobile application support, networking, cryptography, and a general application GUI toolkit are product-scope decisions rather than missing pieces of the current VST3 implementation.

## Capability Matrix

| Area | `zig-vst3` today | Common ecosystem scope | Assessment |
| --- | --- | --- | --- |
| Plugin formats | VST3 plus LV2 core, toolkit-neutral UI, and VSTGUI parameter UI adapters, format-neutral processor and editor lifecycles, and offline and standalone consumers. LV2 covers static f32 audio, plain-unit controls, bounded Atom events, segmented transport, typed Patch Set and Get properties, block-size options, declaration-driven freewheeling mode, latency, activation, parameter and component state, bounded Worker scheduling, bounded Programs core and UI interfaces, generated project, license, maintainer, Patch, preset, Programs, and UI metadata, a linked VSTGUI UI binary, deterministic failure silence, immediate offline and delayed threaded Worker fixtures, repeated UI lifecycle fixtures, independent metadata lint, independent core and UI C ABI checks, and cross-target core and UI fixtures. The AUv2 path covers negotiated lifecycle, f32 and f64 processing, static main and auxiliary buses, reflected parameter metadata, immediate and ramped sample-accurate automation, Core Foundation class-info state, bounded property listeners, pre-render and post-render notifications, timestamp-keyed multi-output rendering, deterministic failure silence, owning component factories, registered Mono Gain and Auxiliary Output Splitter `.component` bundles, and dynamic-library host fixtures. Its portable ABI, dispatch layers, and complete multi-output reference are SDK-checked and cross-built | VST, VST3, AU, AUv3, AAX, and LV2 | The common runtime now has offline, standalone, VST3, LV2, and loadable AUv2 consumers. AU and LV2 real-host validation, general Patch graph operations, remaining LV2 extensions, AUv3, and AAX remain |
| Audio processing | 32-bit and 64-bit processing, checked format-neutral lifecycle, parameter queues and flushes, events, state, latency, tail, bypass, typed and retryable forwarding for all twelve VST3 restart categories, optional resource-path and decoded-audio receivers, scalar, graph, and text telemetry, editor visibility notifications, and VST3 runtime bridging | Mature format-neutral processor abstraction | Strong processor and VST3 core |
| Process modes | Realtime, prefetch, and offline modes reach `prepare` and each process context | Non-realtime processing is exposed through its processor APIs | Covered |
| Bus layouts | Plugin declarations and the allocation-free topology, snapshot, process-context, and wrapper storage models accept a compile-time capacity from zero through 254 auxiliaries per direction. Eight remains the source-compatible default. VST3 supports live negotiation, activation, insertion/removal, immutable realtime publication, backward-compatible versioned state, rollback, and coalesced `kIoChanged` restarts. LV2 and AUv2 publish capacity-selected static ports and buses. Standalone, offline, routing, and native PCM backend paths retain the selected capacity. The 39 non-empty layouts cover mono, five stereo roles, cinematic and music surround variants through 7.1, 5.x and 7.x immersive layouts through 7.1.4, and first- through seventh-order ACN ambisonics | Multiple arbitrary buses, sidechains, a large speaker-layout catalog, and dynamic bus/layout changes | The compile-time bus-count gap is closed. Real-host high-count confirmation remains, and live insertion/removal is format-specific |
| Parameters and state | Typed parameters, sample-accurate changes, bounded binary state, migrations, units, and program lists | Parameters, state trees, undo support, presets, and wrapper integration | Strong core, smaller ecosystem gap |
| MIDI and note data | VST3 events, note expression, legacy MIDI controller assignments, checked MIDI 1 streams and files, RPN, synchronized MPE zones and expressive note state, typed MIDI 1 and MIDI 2 UMP channel messages, Utility, System, SysEx7, SysEx8, Flex Data, Mixed Data Sets, endpoint messages and text, bounded requester and responder endpoint sessions, MIDI-CI connection management, a unified bounded Device registry, delegate-driven local Profile and Property Hosts, Profile Configuration, Property Exchange transport and bounded initiator/responder sessions, subscription and remote value-cache tracking, caller-owned cache snapshots, common JSON headers, Mcoded7, zlib+Mcoded7, ResourceList, foundational resources, Process Inquiry, complete timestamped UMP device contracts and block scheduling, macOS CoreMIDI MIDI 1 device discovery, input, and timestamped output, Linux ALSA UMP discovery, input, and scheduled output, and Windows MIDI Services endpoint discovery and complete-packet I/O | General MIDI streams and files, MPE instruments and zone messaging, MIDI devices, UMP, and MIDI-CI with a unified Device, Profile Host, Property Host, discovered state, and subscription management | Core plugin-facing MIDI and bounded device, host, session, persistent cache, orchestration, and format-neutral UMP transport are covered. Native Linux and Windows UMP are connected. Physical timing and recovery validation remains |
| ARA | Official ARA 2.0–2.3 declarations, exact VST3 companion ABI and vtable-slot checks, fixed-capacity factories with product-owned controller extensions, document controllers, graph ownership, concurrent non-realtime host audio readers, validated analysis and licensing requests, host notifications, host-selectable per-source processing algorithms, typed plugin content readers, bounded typed host source and musical-context content copying, bounded polyphonic note detection with multichannel merging and sub-semitone frequency refinement, bounded general and low-register monophonic equal-temperament tuning detection, streamed constant and piecewise variable-tempo detection with continuous quarter integration, bounded constant and changing simple, odd, and compound meter detection, bounded constant and changing major or minor key-signature detection, bounded sheet-chord detection, approved static-tuning edits, backward-compatible filtered archives with bounded transactional product payloads, role-specific playback and editor renderer interfaces, copied editor-view state, transactional whole-source or LRU-paged f32 or f64 source caches, bounded zero-crossing content-fade analysis, checked overlap-add spectral transformations with immutable generation publication and paged-cache fallback, validated piecewise source-to-context tempo warps, immutable render-plan publication, cached realtime playback with linear, Catmull-Rom cubic, or normalized eight-tap windowed-sinc interpolation, tempo reflection, and linear, smoothstep, or equal-power head and tail fades, main-factory class registration, compile-time product class-list assembly, canonical audio-component aggregation, legacy or role-specific binding, and a validated dual-precision stereo playback reference product | ARA plugin integration and document-controller support | The reusable controller, persisted algorithm-aware music analysis, plugin extension, whole-source and paged cache, content-fade and spectral analyzers, tempo-warp builder, playback renderer, registration, component integration, band-limited resampling, and reference product layers are implemented. Real ARA-host validation remains open |
| DSP | Radix-2 FFT with real and magnitude helpers, windows, checked scalar, fixed-width SIMD, and target-native Padé fast math, fixed-lane and target-native SIMD registers, aligned split-real complex operations and ordinary interleaved complex storage, dispatched copy, gain, affine, add, real and complex pointwise multiply, fast math, weighted mix, common metering reductions, and stereo layout buffer kernels, Bessel I0, complete and incomplete elliptic integrals, all twelve forward real and complex Jacobian functions, three principal real inverses, and principal complex inverse `sn`, windowed and weighted least-squares FIR design, Type I through Type IV equiripple FIR design, fixed-capacity polyphase FIR banks, bounded half-band polyphase all-pass IIR design and processing, FIR, order 1 through 16 Butterworth, Chebyshev Type I and II, and elliptic design with specification-derived orders, direct FIR processing, bounded multichannel 2x through 16x oversampling, biquads, first-order, state-variable, and nonlinear multimode ladder filtering, Linkwitz-Riley splitting, linear and logarithmic smoothing, peak and RMS ballistics, gain, bias, wrapped phase, oscillators, fractional delay, mixing and panning with eight gain laws, waveshaping, chorus, flanger, vibrato, phaser, fixed-offset stereo modulation, checked host transport and tempo following, smoothing for rate, delay, depth, sweep bounds, feedback, and mix, algorithmic reverb, lookup tables, scalar processor chains and duplication, fixed-storage snapshots and reference-counted immutable-generation publication, bounded multichannel blocks with transactional arithmetic and aggregates, replacing and non-replacing process contexts, fixed matrices and polynomials, compression, gating, sample-peak, lookahead, inter-sample, and linked phase-compensated two- through eight-band dynamics, partitioned convolution, denormal control, target-native streaming resampling, fixed-rate conversion, C kernels, and fixture parity tools | Broad DSP support for filters, transforms, convolution, delay, dynamics, modulation, oversampling, and numerical utilities | Useful realtime primitives and the tracked special-function set are covered. Processor-specific smoothing outside the modulation family remains product-driven |
| Audio files | Bounded file-backed PCM WAV, AIFF, uncompressed AIFC, RF64, BW64, and Wave64 reading, caller-buffer and file-backed WAV and AIFF writing, RF64, BW64, and Wave64 writing, shared validated exact or aligned writer checkpoints, counted positional I/O injection with short-write retry and recoverable failures across PCM, FLAC, and Ogg writers, deterministic direct, TPDF, and noise-shaped signed PCM conversion, RIFF INFO and AIFF text metadata, complete BWF `bext`, UTF-8 iXML/aXML chunk framing, typed iXML recorder, speed, timecode, sync-point, file-set, track, history, loudness, BEXT, location, and user data, all ten core ADM identifier families, typed declaration and reference graph validation, profiles, tag groups and targets, exact time values, common block parameters, DirectSpeakers, Objects, Matrix, HOA, and Binaural blocks, allocation-free CHNA track mapping and cross-validation, allocation-free ID3v2.4 and ID3v2.3 tags and frames plus ID3v1.0 and ID3v1.1 tails, registered Wave64 BEXT and LIST/INFO metadata, native bounded FLAC encoding and decoding, incremental file-backed Ogg writing and reading, chained Ogg transport, complete Vorbis header and PCM packet encoding, transactional Vorbis packet-to-PCM stream decoding with overlap and granule trimming, FFmpeg decoder interoperability, bounded memory and file-backed MP3 framing, complete Layer III decoding, stateful PCM spectral analysis, checked CBR and Huffman emission, Xing, Info, LAME, and VBRI metadata, duration scans, and caller-owned positional seek indexes | Readers, writers, streaming, buffering, metadata, and AIFF, FLAC, MP3, Ogg Vorbis, WAV, and platform codecs | Partial |
| Plugin GUI | Toolkit-neutral editor boundary plus optional VSTGUI controls, assets, telemetry, graphs, piano, sequencing, file drop, and accessibility bridges | Full component, layout, graphics, animation, OpenGL, browser, and application-window toolkit | Strong plugin path, not a general GUI framework |
| Accessibility | Native macOS and Windows bridges with automated structural coverage | Cross-platform accessibility integrated with the component framework | Linux native bridge and manual coverage remain |
| Standalone applications | Bounded f32 and f64 offline rendering plus a checked live callback runtime and rollback-safe standalone application lifecycle. Static main and auxiliary buses, flat physical-device routing, bounded timestamp-to-sample MIDI 1 and complete-UMP scheduling, automation, events, output events, transport, deterministic failure silence, callback-failure accounting, directional device discovery snapshots, versioned selection persistence, transactional fallback and restart coordination, preferred-device restoration, transactional multi-source failure monitoring, continuous-phase sinc rate correction, bounded clock-drift fill control, a bounded multichannel SPSC capture FIFO, a synchronized capture-rate processing bridge, a split capture and render processor adapter, bounded top-level window events, macOS CoreAudio with independent input and output AUHAL units, CoreMIDI, and an AppKit window, Windows shared-mode WASAPI with native split capture/render callbacks, timestamp-scheduled WinMM MIDI 1, Windows MIDI Services UMP, and a Win32 window, and Linux ALSA PCM and PipeWire audio with combined and split callbacks, timestamp-scheduled MIDI 1 and UMP RawMIDI, runtime-loaded X11 and Wayland windows, a shared Wayland VST3 host/frame bridge, and a bounded Steinberg run-loop driver share the processor runtime | Standalone plugin wrapper and general application lifecycle | Format-neutral offline and live cores, allocation-free device routing, bounded MIDI scheduling, recovery coordination, failure-triggered same-selection restart, the top-level window shell, Cocoa, Win32, X11, and Wayland window backends, both VSTGUI Linux integration surfaces, both Linux audio paths, and Linux and Windows UMP transports are implemented. Physical PipeWire, UMP timing, disparate-clock, and recovery confirmation remain |
| Plugin hosting | Explicit non-goal | In-process and headless plugin hosting | Intentional non-goal |
| Audio and MIDI devices | Type-erased f32 and f64 audio-device callbacks, checked device configuration, timestamped MIDI 1 and complete-UMP input and output devices, bounded sample-offset scheduling, directional and unified stable identifiers, native or polled topology generations, version-2 selection persistence with version-1 restore, transactional fallback and retry, reappearance recovery, transactional failure-counter aggregation, allocation-free routing, continuous-phase sinc rate correction, bounded clock-drift fill control, a bounded multichannel SPSC capture FIFO, a synchronized capture-rate processing bridge, and a split capture and render processor adapter. CoreAudio supplies AUHAL discovery, runtime properties, combined planar callbacks, directional selection, independent split AUHAL units, and unified or directional device failures. CoreMIDI supplies MIDI 1 endpoint I/O, clock conversion, and retained disconnect failures. WASAPI supplies SHA-256 endpoint identities, independent capture and render defaults, IMMNotificationClient updates, shared-mode conversion, an MMCSS event worker, combined and split planar callback adaptation, bounded legacy duplex capture buffering, failure silence, and retained device failures. WinMM supplies directional MIDI 1 discovery, QueryPerformanceCounter-based input timestamps, a bounded stable-order output scheduler, queue and delivery statistics, topology polling, and retained input and output errors. Windows MIDI Services supplies endpoint-device identities, active function-block directions, complete raw packet input and timestamped output, topology refresh, and retained statistics. ALSA PCM supplies runtime-loaded directional audio discovery, float and double PCM, topology polling, combined processing, independent split capture and playback workers, optional realtime priority, stream recovery, failure silence, and retained device failures. PipeWire supplies runtime-loaded bounded node discovery, stable directional identities, default or named targeting, polled topology generations, planar f32 or f64 streams, bounded combined-duplex capture buffering, independent split callbacks, failure silence, and retained graph failures. ALSA RawMIDI supplies directional MIDI 1 discovery, fragmented and running-status input, monotonic read timestamps, a bounded monotonic-clock output scheduler, queue and delivery statistics, topology polling, and retained input and output failures. ALSA UMP supplies directional endpoint discovery from active function blocks, complete packet reconstruction, monotonic read timestamps, a bounded monotonic-clock output scheduler, topology polling, and retained input and output failures | Device enumeration, callbacks, routing, and platform backends | Format-neutral contracts plus macOS audio and MIDI 1, Windows shared-mode audio, timestamp-scheduled MIDI 1, and native UMP, and Linux ALSA and PipeWire audio, timestamp-scheduled MIDI 1, and native UMP are implemented. Scheduler timing validation on physical devices, disparate-clock validation, and physical recovery confirmation remain |
| Platforms | Desktop VST3 bundles for macOS, Windows, and Linux | Desktop plus iOS and Android application targets | Mobile is out of current scope |
| General utilities | Focused plugin, realtime, resource, and serialization utilities | Networking, OSC, data structures, cryptography, and broad platform services | Out of current scope |

## Product Areas Outside The Plugin Core

The following areas are absent or intentionally much narrower here:

- Undoable state trees, undo management, and general application data models.
- Audio-source, transport, synthesis, sampler, and mixer abstractions.
- Animation, image codecs, OpenGL integration, embedded browser views, and general windowing.
- JavaScript embedding, OSC, analytics, product unlocking, video playback, and camera capture.
- Networking, cryptography, interprocess communication, process launching, and general operating-system services.
- Cross-platform MIDI device discovery and streaming beyond desktop MIDI 1.

These omissions should not all become roadmap items. Most serve standalone applications, hosts, content tools, or licensing systems rather than a VST3 plugin runtime.

## Focused DSP Gaps

The current DSP package still has meaningful capabilities that are missing or intentionally narrower:

| Capability | Current difference |
| --- | --- |
| High-order filter design | Butterworth, Chebyshev Type I, Chebyshev Type II, and elliptic low-pass and high-pass cascades from order 1 through 16 are covered. Passband and stopband specifications derive the minimum bounded order and family-specific critical frequency. Weighted least-squares and minimax equiripple linear-phase FIR design support up to 255 taps through bounded QR and exchange solvers. Equiripple design covers Type I through Type IV symmetry, including constrained DC and Nyquist endpoints. Fixed-capacity FIR prototypes can be decomposed, reconstructed, and processed as polyphase banks. Half-band polyphase IIR design provides as many as 32 stable all-pass sections, analytic low/high response evaluation, and a unity-gain two-path processor |
| Fast math | Checked scalar, fixed-width vector, target-native vector, transactional in-place buffer, and runtime-dispatched forms cover the documented limited-range Padé family for hyperbolic, trigonometric, exponential, and `log(1 + x)` operations. Accuracy vectors and release benchmarks cover the public paths. Broader approximation families remain optional |
| SIMD | Fixed-lane and target-native f32 and f64 registers cover checked aligned and unaligned access, arithmetic, roots, extrema, comparisons, masks, selection, reductions, and lane access. Split-real complex registers add checked aligned plane access. Runtime scalar, AVX2, or NEON dispatch covers copy, gain, affine, add, real and split-complex pointwise multiply, checked Padé fast math, weighted two-buffer mix, sum, inner product, absolute peak, minimum, maximum, sum-of-squares, and overflow-stable RMS. Complex multiplication validates the complete input before mutation, supports exact paired aliases, handles scalar tails, and rejects shifted or cross-plane overlap. Stereo planar/interleaved conversion uses the selected backend width with checked non-overlap. A reusable processor dispatcher selects product-supplied scalar, NEON, or AVX2 callbacks with scalar fallback, forced-backend testing, exact in-place replacement, arbitrary alignment, and scalar-tail contracts. Multi-input and multi-output processor policies remain product-specific |
| Shared processor state | A bounded wrapper combines caller-owned immutable shared state with independent per-channel processor histories. A three-slot publisher provides mutable snapshots for one non-realtime writer and one realtime reader. A second fixed-storage publisher provides reference-counted immutable generations for one writer and multiple concurrent readers with bounded nonblocking acquisition, retention, release, slot reclamation, and transactional partial mutation copied from the active generation. Heap-owned polymorphic processor state is not covered |
| Special functions | Bessel I0, complete elliptic integral K, incomplete elliptic integral F, all twelve standard forward real and complex Jacobian functions, principal amplitude, the three principal real inverse branches, and principal complex inverse `sn` are implemented for f32 and f64 with real parameters. Parameter-based AGM evaluation preserves the finite quarter period at the closest representable parameter below one, bounded period reduction contains maximum finite arguments, and quotient evaluation reports unresolved pole neighborhoods |
| Matrices | Fixed vectors cover checked arithmetic, dot product, stable magnitude, and normalization. Fixed matrices cover addition, scaling, transpose, identity, matrix and vector multiplication, and partial-pivot solving. Allocator-owned dynamic matrices cover checked runtime dimensions, indexing, cloning, identity, addition, subtraction, scaling, transpose, and rectangular multiplication. Reusable fixed and dynamic LU decomposition solves vectors and multiple right hand sides and computes determinants and inverses. Fixed and dynamic Householder QR reconstructs tall matrices and solves full-rank least squares without normal equations. Compact fixed and dynamic one-sided Jacobi SVD covers tall, square, and wide matrices with sorted singular values, numerical rank, reconstruction, condition reporting, pseudoinverses, and minimum-norm overdetermined, rank-deficient, or underdetermined least squares. Dynamic caller-buffer solves use explicit workspaces and preserve their destinations after failure. Allocating operations are non-realtime |
| Polynomials | Bounded evaluation, derivative, integral, addition, subtraction, multiplication, composition, quotient and remainder division, distinct-point interpolation, QR full-rank least-squares fitting, SVD rank-aware minimum-norm fitting, and allocation-free complex root solving are covered. Checked orthogonal-family construction includes Legendre, Chebyshev first and second kind, physicists' and probabilists' Hermite, ordinary and generalized Laguerre, and general Jacobi polynomials. Generalized Laguerre and Jacobi parameters use the finite greater-than-minus-one orthogonality domain. The root solver trims trailing zero coefficients, handles linear polynomials directly, reports convergence, and applies deterministic ordering |
| Oversampling | Bounded single-channel and multichannel pipelines support runtime lists of equiripple FIR, polyphase IIR, and dummy stages. Real stages accept independent up/down transition and attenuation specifications, double the rate from 1x through 256x, report fractional latency, and optionally add exact integer latency. FIR stages automatically select and verify a bounded tap count. Reconfiguration is transactional between complete blocks | Stage-list storage has a compile-time maximum of one through eight entries |
| Convolution | Partitioned and zero-latency convolution, safe resource publication, independent-stereo or mono routing, and a bounded SPSC preparation queue are covered. The queue leaves worker-thread or message-loop ownership to the product |
| Dynamics and modulation | Chorus, flanging, vibrato, phasing, fixed-offset stereo LFO coordination, checked host tempo and musical-phase extraction, host transport following, straight, dotted, and triplet tempo divisions, and sample-bounded smoothing for rate, delay, depth, phaser sweep bounds, feedback, and mix are covered. Reusable linear and positive-domain multiplicative smoothers support arbitrary plugin parameters with exact-duration ramps, bounded skipping, transactional retargeting, and exact endpoints. Compressor, gate, and limiter configuration rejects derived envelope coefficients that cannot advance at the requested finite rate and duration. Fixed-capacity lookahead compression, lookahead and inter-sample limiting, and channel-linked, phase-compensated Linkwitz-Riley compression across two through eight bands are also covered. Processor-specific smoothing policy remains product-driven |
| Audio files | Bounded file-backed PCM WAV, AIFF, uncompressed AIFC, RF64, BW64, and Wave64 reading, caller-buffer WAV and AIFF writing, file-backed streaming WAV and AIFF, and 64-bit RF64, BW64, and Wave64 output are available. One shared endian-aware codec provides signed PCM16, PCM24, PCM32, and IEEE f32 conversion, normalized decoding, and fixed-storage positional write staging. A deterministic per-channel quantizer covers direct, TPDF, and first-order noise-shaped conversion from 2 through 32 bits. Caller-buffer WAV and AIFF support one-shot and incremental 16-, 24-, and 32-bit dither, and all file-backed PCM writers support transactional dithered append for their integer encodings. Shared checked checkpoints cover exact FLAC and Ogg boundaries, RIFF and AIFF word alignment, and Wave64 eight-byte alignment while preserving container-specific commit rules. RIFF INFO and AIFF text metadata codecs preserve known and unknown FourCC entries. The full version-aware BWF `bext` layout and UTF-8 iXML/aXML chunk framing are covered, and WAV, RF64, and BW64 readers can retrieve these chunks into caller storage. Typed iXML helpers cover production-recorder fields, take flags, file identity, exact speed and timecode ratios, sample-rate and split-timestamp values, relative and absolute sync points, event durations, file-set grouping, track lists, derivation history, native and BEXT loudness, portable BEXT values, bounded GPS location data, legacy user text, and standardized iXML 2.0 AFSI user fields while ignoring structurally nested unknown extension subtrees. ADM support covers all ten core identifier families, common and custom format-definition classification, exact CHNA records, track and direct-channel references, optional pack references, repeated UIDs per track, complete track coverage, reserved zero entries, case-insensitive duplicate detection, audio channel-count agreement, and aXML plus CHNA carriage in WAV, RF64, and BW64. The bounded typed XML graph resolves namespace URIs, handles adjacent escaped text and literal CDATA sections, matches typed elements to the `audioFormatExtended` namespace, preserves owned foreign extension subtrees and attributes, classifies same-namespace untyped subtrees and prefixed attributes, and recognizes every declaration family, validates reference kinds and owners, local custom definitions, type-label compatibility, identifier continuity, cardinality, stream and track reciprocity, and CHNA consistency. Profile declarations expose the required generic fields. The BS.2168 emission-profile subset identifies the exact version and levels, rejects duplicate declarations, requires the BS.2076-3 document version and core element families, enforces level-specific top-level and direct programme/content/object sub-element maxima, excludes track and stream formats, restricts local pack and channel formats to Matrix and Objects, enforces local identifier floors and ordered track UIDs, checks the one-to-one content-to-object identifier rule, requires every content to be reached directly by a programme, assigns every object exactly one structural parent, permits object nesting through level two, rejects cycles, requires parent-matched consecutive alternative-value-set IDs, enforces leaf and branching object source rules, recognizes every permitted polar and Cartesian common DirectSpeakers pack, applies level channel limits, validates exact pack and channel backreferences with one-to-one coverage, rejects silent tracks, and requires every local Objects pack, local channel, and track UID to be used. Its downmix validator checks Matrix input/output topology, unique common-layout pairs, side-information separation, one-block channels, injective output and coefficient mappings, gain bounds, and Matrix-specific allowed structure. Tag groups retain classes and typed programme, content, or object targets. Exact decimal and sample-fraction time values drive typed common block validation. DirectSpeakers, Objects, Matrix, HOA, and Binaural blocks expose their type-specific fields and constraints, including legacy Matrix output references in the generic model and Binaural ear names. Allocation-free ID3v2.4 framing covers generic known and unknown frames, syncsafe sizes, status and format flags, extended headers, padding, footers, per-frame unsynchronisation, caller-buffer decoding, and the four standard text encodings. A separate ID3v2.3 path covers ordinary frame sizes, its version-specific flags and auxiliary fields, extended headers, CRC retention, declared padding, tag-wide unsynchronisation, caller-storage decoding, and Latin-1 or BOM-qualified UTF-16 text. Fixed ID3v1.0 and ID3v1.1 tails cover their bounded text, genre, and track fields. Compression and encryption flags are retained without embedding algorithm policy. Wave64 supports its registered BEXT and LIST/INFO GUID chunks without assigning proprietary GUIDs to RIFF XML, CHNA, or ID3 data. Native FLAC covers bounded incremental input, output, metadata, seeking, checksums, and all standard subframes and stereo depths. Ogg version 0 page and packet framing covers CRC, lacing, continuation, sequential chained streams, caller-buffer assembly, incremental file output with rollback recovery, and positional file input. Vorbis support parses and retains every header component, decodes complete audio packets through floors, residues, coupling, floor application, precomputed windows, and FFT-based inverse MDCT, then performs atomic multichannel overlap and signed granule trimming. Its bounded encoding path now composes PCM framing and analysis, normalized forward MDCT, psychoacoustic thresholds, setup-driven Floor 1 fitting, residue normalization and coupling, adaptive mapping-driven quantization, exact rate preflight, final packet encoding, and atomic sequence append. A bounded stream decoder handles priming, PCM positions, short streams, final-frame trimming, reset, and EOS. Caller-owned memory and file indexes retain safe overlap-prime packet locations, transactional file seeking validates those locations, and a decode-forward cursor clips exactly to the requested signed PCM position. Bounded MP3 framing covers MPEG-1, MPEG-2, and MPEG-2.5 Layer III headers, ordinary and free-format frame lengths, bounded two-boundary free-format inference with padding, ID3 boundaries, Xing, Info, LAME, and VBRI metadata, encoded duration scans, positional file reads, and caller-owned memory and file seek indexes. Static and delayed Matrix coefficients, bounded caller-matrix HOA decoding, direct-ear binaural mixing, common-layout and position-based DirectSpeakers, polar and Cartesian point-source rendering, zero-extent Objects gain planning, and polar and Cartesian Objects extent rendering, bounded typed exclusion zones with polar gain-power redistribution and Cartesian reduced-layout exclusion, plus bounded exact-time Objects and Binaural gain timelines and label-stable diffuse decorrelation with matched direct delay are available. Remaining production profiles, external loudness measurement accuracy, edited-flow policy, variable Matrix coefficient policy and phase processing, HOA loudspeaker-matrix generation, HRTF synthesis, independent Vorbis reference parity and third-party interoperability, MP3 VBR, intensity-stereo, and broader encoder policy, platform codecs, DTD handling, and typed vendor iXML schemas remain |

The MP3 summary in the table is superseded by the dated 2026-07-30 gates. Complete Layer III decoding, independent PCM conformance, and gapless range handling are implemented. Encoding covers checked CBR headers and padding, CRC-protected frames, exact side-information and scale-factor serialization, all pair and count1 Huffman codebooks, non-byte-aligned payload packing, and transactional quantized-frame output. Stateful polyphase analysis, forward hybrid MDCT, encoder alias preparation, exact pure-short and mixed-short transmission reordering, synchronized joint-stereo block classification, checked mid-side and high-band intensity transformation, bounded per-band energy, spectral-flatness tonality, adjacent-band masking, and optional forward temporal masking feed energy-weighted shared frame budgeting and reconstruction-error scale-factor allocation. Intensity policy covers long, pure-short, mixed-short, MPEG-1, MPEG-2, and MPEG-2.5 layouts and composes with CBR or VBR selection. Global-gain quantization includes exact factor-plus-Huffman budgeting, automatic long, switched, short, and mixed pair regions, automatic pair and count1 codebooks, and one-call PCM-to-frame composition. Per-channel masking history is transactional, resets explicitly, restarts across unmatched block layouts, and advances once per input frame even when VBR evaluates multiple candidates. A bounded one-frame pending encoder moves the following frame's main data into prior spare capacity, updates the byte-granular backpointer, and preserves protected-frame checksums. Its stream and recoverable file paths retain exact delayed-commit counts. Bounded VBR evaluates legal bitrate indexes in ascending order, measures each retained quantization against the masking threshold, and reports whether the configured quality bound was met. One padding accumulator remains continuous across bitrate changes. Sequential CBR and VBR encoding flushes the 1,057-sample analysis delay, reports the exact 95-sample end padding, validates cumulative state, and writes complete frames positionally with retryable checkpoint recovery after partial writes. Reserved Info and Xing frames carry final counts, byte totals, delay, padding, optional quality, and a caller-bounded 100-entry VBR seek table without replacing user PCM. A generated protected joint-stereo fixture decodes through FFmpeg to nonsilent 44.1 kHz stereo PCM. VBR-reservoir composition, broader metadata policy, a wider interoperability matrix, and audition remain.

Vorbis encoding is no longer an all-or-nothing gap. The package serializes headers and setup-driven audio packets, plans variable blocks with bounded lookahead, analyzes PCM through a normalized forward MDCT and Bark-domain masking, fits Floor 1 data, plans coupled residue codewords, preflights exact packet and reservoir state, and publishes memory or file output transactionally. FFmpeg decodes the generated fixture to nonsilent PCM in the automated gate. AudioToolbox did not expose a Vorbis decoder in the current environment, so that path is skipped and not claimed. Independent Xiph parity, a broader decoder matrix, external-encoder inputs, and audition remain.

Complementary-object topology, programme inclusion, source-type consistency, derived profile-level counts, parameter equality, and programme-selected alternative-value-set consistency are implemented. Object interaction ranges, gains, position offsets, and alternative-value-set overrides are validated before cross-member comparison. Programme and content metadata validation requires bounded names, registered ISO 639-2 languages, unique localized-label languages, exact integrated or dialogue loudness structure, valid dialogue kind enumerations, and the profile's permitted attributes and direct children. Local Matrix and Objects packs and channels require bounded names, matching type declarations, type-specific child sets, and valid block cardinality. Track UIDs accept only their profile attributes and exact pack and channel references. File block parameters and timing, complementary labels, opt-in label-language consistency, the recommended `und` programme language for complete multilingual complementary groups, and recommended dialogue-loudness presence for dialogue-bearing content and programmes are implemented. Explicit PCM essence validation covers physical track count, optional sample rate and bit depth, and exact Objects block coverage through the file duration. S-ADM validation covers the BS.2125-1 frame, header, frame format, transport declarations, physical PCM track records, one-to-one ADM track UID mapping, duplicate-free header profiles, semantic equality with embedded emission profiles, per-channel local Objects block initialization, continuity, counter, duration, and frame coverage, and caller-owned original-flow continuity. Allocation-free Matrix rendering binds typed coefficients to explicit input channel identifiers, converts linear or dB gain, supports caller-bounded constant delay with the specified nearest-sample rounding, preflights buffer shapes, and contains non-finite samples. Common-layout mapping precedes exact-label and position-based DirectSpeakers routing. It recognizes the supported common input packs, applies the ordered symmetric gain rules only when every destination label exists, and enforces the layout-specific high-channel and LFE restrictions. The ordinary path handles standard labels and URNs, LFE classification, polar or allocentric Cartesian bounds, circular azimuth ranges, polar and Cartesian screen transforms, unique nearest-speaker selection, discard behavior, and explicit point-source fallback. The Cartesian point-source panner uses a separable three-dimensional dual-balance algorithm over caller-owned allocentric layouts. The polar point-source panner triangulates nominal positions, warps regions to measured positions, handles triplet and quadrilateral facets, virtual layer and pole downmixes, and the dedicated stereo downmix. Both allocation-free panners exclude LFE outputs, support f32 and f64, preserve their specified power behavior, and preflight block mixing. The static Objects point plan adds coordinate conversion, default or explicit reference-screen scaling, screen-edge locking, egocentric or allocentric channel lock, divergence, block gain, direct/diffuse splitting, LFE suppression, and transactional two-bus mixing. The polar extent path precomputes a 1,652-direction spreading table in caller storage, applies rounded width and height weighting with distance and depth modification, and composes with the same polar Objects transforms and gain stages. The Cartesian extent path evaluates separable fixed virtual-source grids, applies dimensional inside and boundary weighting plus room-boundary fading, and composes with the Cartesian Objects transforms and gain stages. Zone exclusion selects nominal speakers from bounded polar or Cartesian regions, applies priority-grouped power redistribution after polar panning, and removes repaired allocentric rows before Cartesian channel lock and panning. Bounded Objects timelines precompute point or extent endpoint gains and apply exact-time ordinary, immediate-jump, or shortened interpolation across arbitrary sample partitions. Bounded Binaural timelines retain independent ear-channel sequences and apply the same exact-time transition semantics while replacing stereo output across arbitrary partitions. External loudness measurement accuracy, edited-flow policy, variable Matrix coefficient policy and phase processing, HOA loudspeaker-matrix generation, HRTF synthesis, and external serial-tool confirmation remain incomplete.

The dated 2026-07-30 variable Matrix gate supersedes the older Matrix gap statements above. A separate opt-in mixer binds named metadata variables to bounded absolute-sample control lanes for linear gain, phase degrees, and delay milliseconds. Lanes use held or linear interpolation and preserve exact sample offsets beyond floating-point integer precision. Delay endpoints retain the standards-conformant nearest-sample rule, with fractional interpolation between endpoints. Phase rotation uses a caller-designed antisymmetric quadrature FIR, aligns every Matrix term to its group delay, and reports that latency. The constant-gain and integer-delay mixer remains unchanged for the renderer-standard path. HOA loudspeaker-matrix generation, HRTF synthesis, external renderer comparison, and audition remain incomplete.

The dated 2026-07-30 HOA generator gate supersedes the loudspeaker-matrix gap statements above. A bounded non-realtime generator evaluates the declared real N3D, SN3D, or order-three FuMa components at caller-defined full-sphere speaker directions and computes a minimum-norm SVD pseudoinverse. Basic and max-rE order weights are available. Rank, condition number, exact component binding, duplicate-direction rejection, LFE silence, and direct construction of the existing realtime matrix decoder are covered. Near-field radial filtering, screen-referenced HOA, HRTF synthesis, independent renderer comparison, and physical loudspeaker audition remain incomplete.

The dated 2026-07-30 expanded HRTF gate supersedes the synthesis gap statements above. Fixed-capacity measured stereo responses, fractional delays, full-sphere direction validation, exact nearest selection, normalized three-neighbor inverse-distance, delay-aligned, and optional spectral interpolation are covered. Static rendering uses immutable filter publication with partitioned or zero latency. Scheduled rendering transforms world-space source and head poses, precomputes every filter, and performs smooth absolute-sample crossfades without allocation. An optional runtime-loaded netCDF boundary validates SimpleFreeFieldHRIR 1.0 through 1.2. Synthetic conversion fixtures and the official 1,250-direction public test file pass. Product-specific dataset selection, live tracker transport, room response composition, broader public-dataset coverage, and headphone audition remain incomplete. The dated dynamic-decomposition and polynomial-family gates also supersede the older DSP summary: allocator-owned checked matrices with reusable LU, QR, and SVD objects and the Legendre, Chebyshev, Hermite, Laguerre, and Jacobi families are implemented.

## Recommended Order

The most valuable capability sequence for plugin authors is:

1. Complete external-host validation for the VSTGUI LV2 UI and core, then add only the LV2 extensions required by real products.
2. Validate Cocoa, Win32, X11, Wayland, both Linux VSTGUI paths, PipeWire, the native MIDI schedulers, and Windows MIDI Services physically.
3. Validate polyphonic-note, tuning, constant or variable-tempo detection, spectral transformations, approved overrides, archive restoration, and playback in external ARA hosts.
4. Expand DSP, audio-file, specialized speaker-layout, and bus capacities when real workloads require them.

AUv2, AUv3, AAX, and LV2 are not thin aliases for VST3. The LV2 core, Programs interfaces, UI bridge, and VSTGUI parameter backend prove one additional wrapper shape. The AUv2 runtime, component factory, state and automation dispatch, registered bundles, SDK checks, and dynamic host fixtures prove another. LV2 and AUv2 still need real-host coverage. AUv3 needs separate Apple platform integration, while AAX has external SDK and distribution requirements.

## Scope Recommendation

Plugin hosting, device management, mobile application infrastructure, and a general desktop GUI framework should remain non-goals unless the project is deliberately re-scoped. They do not improve the VST3 authoring path enough to justify their maintenance burden today.

The bounded topology, snapshot, process-context, wrapper, and device-backend types support compile-time capacities through 254 auxiliaries in each direction. Compatibility declarations and accessors preserve the original single-sidechain and single-output API, and eight remains the default capacity. Runtime VST3 mutations are published to processing, persisted, and synchronized through host restarts. LV2 and AUv2 publish their selected static topology. The expanded layout catalog covers 39 non-empty layouts and preserves every legacy persisted identifier. High-count configurations still need confirmation in real hosts.

Standard MIDI File support now covers formats 0, 1, and 2, metric and SMPTE timing, tempo-aware tick conversion, running status, channel messages, meta events, SysEx events, and bounded writing into caller storage. MPE zone layouts validate lower and upper channel allocation, synchronize through RPN 6, and apply pitch-bend ranges through RPN 0. The bounded MPE instrument tracks note identity, lifecycle, sustain, pressure, timbre, combined manager and member pitch bend, and configurable same-channel expression targeting. The sender allocator provides deterministic free-channel choice and explicit oldest-note stealing.

UMP support now validates all Message Type packet lengths, owns packets in fixed four-word storage, and iterates mixed-size packet streams. It covers typed MIDI 1 and MIDI 2 channel voice, System Common and Real-Time, current Utility messages, SysEx7, SysEx8, Mixed Data Sets, fixed endpoint and function-block messages, MIDI Clip boundary markers, segmented endpoint text, fixed Flex Data setup events, chord names, and all standardized Flex text statuses. Packetizers borrow caller storage, and reassemblers use fixed capacity with transactional sequence checks. Bounded requester and responder sessions cover endpoint discovery, configuration negotiation, static function blocks, segmented names, and outstanding replies. MIDI-CI connection management covers versioned Discovery, Invalidate MUID, Endpoint Information, ACK, and NAK messages. The unified bounded Device owns discovered remotes, negotiated capabilities, endpoint identities, profile snapshots, Property Exchange sessions, subscriptions, remote property values, bidirectional inquiry responses, and disconnect cleanup. Its outgoing Get path validates negotiated support and caches successful replies. The standalone remote cache supports transactional replacement, stable generations, resource IDs, capacity errors, per-device release, and versioned snapshots in caller storage. Restore validates the complete snapshot before publishing any state. The local Profile Host owns bounded profile state, answers inquiries, applies delegate-approved enablement changes, returns details, and dispatches profile-specific data. The local Property Host reassembles requests, parses strict headers, dispatches Get, Set, and subscription policy to a typed delegate, and updates subscription state transactionally. Property Exchange also covers capability negotiation, bounded initiator and responder caches, request and reply chunk sequencing, timeout notifications, strict common JSON headers, Mcoded7, zlib+Mcoded7, ResourceList, DeviceInfo, ChannelList, links, and JSON Schema documents. Process Inquiry covers capability negotiation and correlated MIDI Message Report request, Begin, and End envelopes. Plugins still provide profile-defined payload semantics and the device-specific message snapshots carried inside a report. Device I/O and resource-specific models beyond the foundational set remain.
